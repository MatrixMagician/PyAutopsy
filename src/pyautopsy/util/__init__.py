"""Pure-Python utility tier (no native dependencies).

Holds cross-cutting helpers shared by every higher tier: the UTC-everywhere
timestamp helper (``timeutil``) and path confinement for evidence-controlled
names (``confine``).
"""
