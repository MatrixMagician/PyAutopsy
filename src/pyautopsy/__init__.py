"""PyAutopsy: forensically-sound disk-image ingest on The Sleuth Kit.

The package version defined here is the single source of truth: it is read by
the hatchling build backend (``[tool.hatch.version]``) and exposed to runtime
code (and the audit/chain-of-custody record) via ``pyautopsy.__version__``.
"""

__version__ = "0.1.0"

__all__ = ["__version__"]
