---
milestone: v1.0
audited: 2026-06-01T12:30:00Z
status: tech_debt
scores:
  requirements: 27/27
  phases: 5/5
  integration: 6/6
  flows: 6/6
gaps:
  requirements: []   # No unsatisfied or orphaned requirements
  integration: []    # 0 broken/partial cross-phase links
  flows: []          # 6/6 E2E flows wired
nyquist:
  compliant_phases: ["01-forensic-foundation"]
  partial_phases:
    - "02-filesystem-walk-metadata"
    - "03-timeline-mvp-report"
    - "04-deleted-recovery-known-file-filtering"
    - "05-log-parsing-supertimeline-search"
  missing_phases: []
  overall: partial
tech_debt:
  - phase: 01-forensic-foundation
    items:
      - "Manual-only (documented, not a gap): real E01 ingest on a libewf-equipped host — CI host has no libewf/pyewf; adapter exercised against mocked pyewf.handle."
  - phase: 03-timeline-mvp-report
    items:
      - "Missing regression test: fresh-case (A2) guard is implemented (analyze.py:203-208) but test_analyze_refuses_existing_case was never written (plan 03-03 asserted it)."
      - "IN-04: build_run_metadata durations={} always empty — per-stage timings never recorded (non-analytical, outside determinism contract)."
      - "IN-01/02/03: truncation disclosure rendered twice; explode=_explode alias; repr(sorted(...)) dedup idiom fragile (cosmetic)."
  - phase: 04-deleted-recovery-known-file-filtering
    items:
      - "WR-02: hard-coded fixture inode ground-truth addresses are unverified (test-quality robustness; production behavior unaffected)."
      - "IN-02: stale non-editable site-packages install can mask the fix in an ad-hoc REPL (environment hygiene)."
  - phase: 05-log-parsing-supertimeline-search
    items:
      - "WR-05: RFC3164 grammar requires a host token; hostless/relay syslog lines fall to the unmatched bucket (degrades gracefully)."
      - "WR-06: dateext negative-index ordering/completeness bug — affects dateext rotation only; numeric rotation correct."
      - "GR-WR-01: _seed_year_from_mtime uses getattr(row,'mtime',0) swallowing a default (robustness nit on current typed seam)."
      - "GR-WR-02/04: per-history unconditional completeness/tamperability findings produce low-signal boilerplate; findings_count counts sets+histories, not substantive anomalies (framing nit)."
      - "GR-WR-03: findings_for re-parses the history file a second time (harmless — tamperability is a constant string)."
      - "WR-01/02/03/04 + IN-01..05: ioc_terms test-only reach; hash-arm fallback unreachable; inline read-only SELECT boundary nits; pre-open mount guard fires before audit log built (rejection clean but unlogged); cosmetic items."
  - process: nyquist-validation
    items:
      - "Phases 02–05 have VALIDATION.md files in status: draft with nyquist_compliant: false. Wave-0 validation was not formally closed for these phases (discovery only — never blocks; phases verified passed by other means)."
---

# Milestone v1.0 — Audit Report

**Milestone:** v1.0 (PyAutopsy — automated Linux digital-forensics CLI on pytsk3/TSK)
**Audited:** 2026-06-01
**Status:** `tech_debt` — all requirements met, no blockers, accumulated non-blocking debt for review before archiving.

## Verdict at a glance

| Dimension | Score | Result |
|-----------|-------|--------|
| Requirements satisfied | **27 / 27** | ✓ all v1 requirements covered, no orphans |
| Phases verified `passed` | **5 / 5** | ✓ every phase has a passing VERIFICATION.md |
| Cross-phase integration | **6 / 6 flows** | ✓ 0 broken/partial links |
| E2E `analyze` pipeline | **wired** | ✓ one coherent pipeline, not five silos |
| Nyquist validation coverage | **1 compliant / 4 partial** | ⚡ draft VALIDATION.md on phases 2–5 |
| Critical blockers | **0** | ✓ none |

The milestone achieved its definition of done. The `tech_debt` status reflects an
accumulated set of explicitly-deferred, verifier-judged non-blocking items plus
incomplete Nyquist wave-0 closure on four phases — surfaced here for a review
decision, not because anything blocks completion.

## Definition of Done (ROADMAP)

PyAutopsy v1.0 turns an acquired disk image (plus its logs) into a defensible,
presentation-ready forensic report through a single automated Python/Linux CLI on
The Sleuth Kit — read-only, hash-verified, audited, with deleted-file recovery,
known-file filtering, log parsing into a shared event model, a UTC super-timeline,
and keyword/IOC search. All five phase goals are verified met.

## Requirements Coverage (3-Source Cross-Reference)

Every v1 REQ-ID was cross-referenced across (a) phase VERIFICATION.md status, (b)
plan SUMMARY.md `requirements-completed` frontmatter, and (c) the REQUIREMENTS.md
traceability table (`[x]`). All three agree for all 27 requirements.

| Phase | Requirements | VERIFICATION | SUMMARY frontmatter | Traceability | Final |
|-------|-------------|--------------|---------------------|--------------|-------|
| 1 Forensic Foundation | INGEST-01..04, REPORT-01, REPORT-02 (6) | passed | listed (01-00/01/02/03/04) | `[x]` | **satisfied** |
| 2 Filesystem Walk & Metadata | META-01..05 (5) | passed | listed (02-00/01) | `[x]` | **satisfied** |
| 3 Timeline & MVP Report | TIME-01, REPORT-03, REPORT-04, CLI-01, CLI-02 (5) | passed | listed (03-01/02/03) | `[x]` | **satisfied** |
| 4 Deleted Recovery & Filtering | RECOV-01..03, FILTER-01 (4) | passed | listed (04-00/01) | `[x]` | **satisfied** |
| 5 Log/Super-Timeline/Search | LOG-01..04, TIME-02, SEARCH-01, SEARCH-02 (7) | passed | listed (05-00/01/03/04/06) | `[x]` | **satisfied** |

- **Satisfied:** 27 / 27
- **Unsatisfied:** 0
- **Orphaned** (in traceability but absent from all VERIFICATIONs): 0

## Phase Verification Roll-Up

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 01 Forensic Foundation | ✓ passed | 5/5 SC, 24/24 truths | 3 Critical + 7 Warning review fixes confirmed in code; 1 documented manual-only E01-on-libewf check |
| 02 Filesystem Walk & Metadata | ✓ passed | 5/5 | Both prior human-verification items resolved on host-built fixtures (multi-FS partition tagging; sub-second MACB fold) |
| 03 Timeline & MVP Report | ✓ passed | 5/5 SC | CR-01 + WR-01..05 fixed; 3 human-UAT items resolved (large-image truncation, A4 PDF, live integrity states) |
| 04 Deleted Recovery & Filtering | ✓ passed | 16/16 | RECOV-02 root-deletion misclassification gap closed (plan 04-04) and regression-pinned both directions |
| 05 Log/Super-Timeline/Search | ✓ passed | 16/16 | HUMAN-UAT gaps G-2 (tamperability/completeness disclosures) + G-1 (fixture year reconciliation) closed; 220 tests pass |

No phase is missing a VERIFICATION.md. No phase reports `gaps_found`.

## Cross-Phase Integration (gsd-integration-checker)

**Score: 6 / 6 flows WIRED — 0 broken/partial links.**

| # | Flow | Status | Evidence |
|---|------|--------|----------|
| 1 | Master `analyze` pipeline composition (CLI-01) | ✓ WIRED | `core/analyze.py:run_analyze` composes ingest→walk→opt(recover/filter)→build_timeline→opt(logs/search)→assemble→json+html in one process; CaseStore sole writer via `transaction()`. |
| 2 | Super-timeline merge fs + log (TIME-02) | ✓ WIRED | fs MACB + log events use the same `insert_timeline_events` + same `explode()`; build_timeline runs before run_logs (no double-insert); single total-order ORDER BY (`store.py:737`). |
| 3 | Report reads all producer tables (REPORT-03/04) | ✓ WIRED | `assemble_report_body` reads files, volume_limitations, timeline_events, recovery, known_file_matches, log_findings, search_hits; HTML + JSON render every section. |
| 4 | D-14 native-seam invariant | ✓ WIRED | only `evidence/image.py` + `evidence/filesystem.py` import pytsk3/pyewf; enforced by `test_seam_allowlist.py`. |
| 5 | CLI-02 determinism end-to-end | ✓ WIRED | wall-clock segregated to `run_metadata.json` sidecar; body has no timestamp helper; JSON sort_keys; opt-in flags add only content-derived rows. |
| 6 | Read-only forensic soundness across orchestrators | ✓ WIRED | `assert_source_not_mounted` before+after open in ingest/walk/recover/search/logs; reverify lives inside run_ingest only. |

The integration checker confirmed the five individually-verified phases genuinely
compose into one coherent `analyze` pipeline (58 integration tests across analyze /
super-timeline / reproducibility / read-only / seam-allowlist / report / CLI-smoke pass).

## Nyquist Validation Coverage (discovery only)

| Phase | VALIDATION.md | nyquist_compliant | Classification |
|-------|---------------|-------------------|----------------|
| 01 | exists (status: approved) | true | COMPLIANT |
| 02 | exists (status: draft) | false | PARTIAL |
| 03 | exists (status: draft) | false | PARTIAL |
| 04 | exists (status: draft) | false | PARTIAL |
| 05 | exists (status: draft) | false | PARTIAL |

**Overall: partial.** All five phases have a VALIDATION.md, but only Phase 1 is
formally `nyquist_compliant`. Phases 2–5 carry draft VALIDATION docs that were never
closed. This is a *process-completeness* observation — these phases were still
verified `passed` by goal-backward verification + tests + human UAT. If you want the
formal wave-0 validation closed, run `/gsd-validate-phase 2`, `3`, `4`, `5`.

## Tech Debt (non-blocking, deferred — see frontmatter for full list)

All items below were explicitly judged non-blocking by phase verifiers / code
reviews and (for Phase 5) deferred by the user. None defeats a requirement or a core
invariant (D-14 single seam, CaseStore sole writer, read-only soundness, CLI-02
determinism).

- **Phase 3:** missing `test_analyze_refuses_existing_case` regression for the
  (implemented, reachable) fresh-case guard; `durations={}` always empty; 3 cosmetic IN items.
- **Phase 4:** unverified hard-coded fixture inode ground-truth (test quality); stale
  site-packages REPL masking hazard (environment hygiene).
- **Phase 5:** ~15 carried open warnings (RFC3164 hostless lines → unmatched bucket;
  dateext rotation ordering; low-signal boilerplate completeness/tamperability
  findings; `findings_count` framing; assorted reachability/boundary/cosmetic nits).
- **Process:** Nyquist wave-0 not formally closed on phases 2–5 (draft VALIDATION.md).

## Conclusion

PyAutopsy v1.0 meets its definition of done: **27/27 requirements satisfied, 5/5
phases verified, 6/6 cross-phase flows wired, 0 blockers.** The milestone is
completable. The `tech_debt` classification exists only to put the accumulated
non-blocking items and the partial Nyquist coverage in front of you for an explicit
accept-or-address decision before archiving.

---

_Audited: 2026-06-01 — gsd-audit-milestone_
