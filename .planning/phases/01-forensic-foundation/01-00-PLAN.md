---
phase: 01-forensic-foundation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - README.md
  - Containerfile
  - src/pyautopsy/__init__.py
  - src/pyautopsy/util/__init__.py
  - src/pyautopsy/util/timeutil.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/fixtures/make_fixtures.py
  - tests/fixtures/tiny_raw.dd
  - tests/test_timeutil.py
  - tests/test_cli_smoke.py
autonomous: true
requirements:
  - INGEST-01
  - INGEST-02
  - INGEST-03
  - INGEST-04
  - REPORT-01
  - REPORT-02
user_setup: []

must_haves:
  truths:
    - "Running `pytest -q` discovers and executes the test suite from a src layout"
    - "A tz-aware UTC ISO-8601 timestamp helper exists and is the only sanctioned timestamp source"
    - "A tiny deterministic raw image fixture and malicious-archive fixture builders exist for downstream tests"
    - "An end-to-end `pyautopsy ingest` CLI test exists and fails for the right reason (command not yet implemented), pinning the Walking Skeleton target"
  artifacts:
    - path: "pyproject.toml"
      provides: "hatchling build backend, src layout, pytest pythonpath=src, [ewf] extra, ruff+mypy config, pinned pytsk3/pyewf/typer versions"
      contains: "hatchling"
    - path: "src/pyautopsy/util/timeutil.py"
      provides: "utc_now() -> tz-aware datetime and iso_utc() -> ISO-8601 +00:00 string"
      exports: ["utc_now", "iso_utc"]
    - path: "tests/conftest.py"
      provides: "tmp_path case-dir fixture and tiny-raw-image fixture path fixture"
      min_lines: 20
    - path: "tests/fixtures/make_fixtures.py"
      provides: "deterministic builders for the tiny raw image + malicious tar/zip fixtures"
      min_lines: 40
    - path: "tests/test_cli_smoke.py"
      provides: "failing end-to-end ingest smoke test (Walking Skeleton target)"
      contains: "ingest"
  key_links:
    - from: "tests/conftest.py"
      to: "tests/fixtures/tiny_raw.dd"
      via: "fixture returns committed image path"
      pattern: "tiny_raw"
    - from: "src/pyautopsy/util/timeutil.py"
      to: "datetime.now(timezone.utc)"
      via: "tz-aware UTC construction"
      pattern: "timezone\\.utc"
---

<objective>
Stand up the PyAutopsy project skeleton and test infrastructure (Wave 0 of the Walking Skeleton) so every subsequent vertical-slice plan has a working build, a runnable pytest suite, shared fixtures, the UTC-everywhere timestamp helper, and a failing end-to-end `ingest` test that pins the phase target.

Purpose: This is the greenfield foundation. No code, no tests, and no fixtures exist yet (repo holds only `.gitignore`, `LICENSE`, `CLAUDE.md`). Per 01-VALIDATION.md every Phase-1 test is a "Wave 0 gap" — this plan closes those gaps so the requirement→test contract becomes executable. Implements D-13 (src layout, hatchling, pytest, ruff, mypy, pinned versions, README + Containerfile) and D-10 (UTC-everywhere timestamp helper, established from the very first phase per PITFALLS P4).

Output: `pyproject.toml`, `src/pyautopsy/` package root with `util/timeutil.py`, `tests/` tree with `conftest.py` + fixture builders + a committed tiny raw image, README with native-dep install story, a Containerfile, and a deliberately-failing end-to-end CLI smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-forensic-foundation/01-CONTEXT.md
@.planning/phases/01-forensic-foundation/01-RESEARCH.md
@.planning/phases/01-forensic-foundation/01-VALIDATION.md
@.planning/phases/01-forensic-foundation/SKELETON.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold the package, build config, and dev tooling (D-13)</name>
  <files>pyproject.toml, README.md, Containerfile, src/pyautopsy/__init__.py, src/pyautopsy/util/__init__.py</files>
  <read_first>
    - .planning/phases/01-forensic-foundation/01-CONTEXT.md (D-13 scaffolding decision; D-04 [ewf] extra)
    - .planning/phases/01-forensic-foundation/01-RESEARCH.md (§Standard Stack pinned versions; §Recommended Project Structure; §Installation native deps; §Environment Availability)
    - .planning/phases/01-forensic-foundation/SKELETON.md (architectural decisions contract)
    - CLAUDE.md (python-dev skill: PEP 8, 88-char, modern type hints, src layout, hatchling, ruff, mypy)
  </read_first>
  <behavior>
    - `pip install -e .` (or `pip install -e ".[dev]"`) succeeds offline against pinned versions where wheels exist
    - `import pyautopsy` resolves `pyautopsy.__version__` (single source of truth)
    - `ruff check src tests` and `mypy src` run without configuration errors (clean tree)
    - `pytest -q` collects tests from `src` layout via `pythonpath = ["src"]`
  </behavior>
  <action>
    Create `pyproject.toml` with `[build-system] requires = ["hatchling"]`, `build-backend = "hatchling.build"`. Declare `[project]` name `pyautopsy`, `requires-python = ">=3.11"`, and core dependencies pinned per 01-RESEARCH.md §Standard Stack: `pytsk3==20260520`, `typer>=0.12,<1`, `rich>=13`. Declare `[project.optional-dependencies]` with an `ewf` extra holding `libewf-python==20240506` (D-04 — keep core install light; E01 optional) and a `dev` group with `pytest`, `ruff`, `mypy`. Declare the console entry point `[project.scripts] pyautopsy = "pyautopsy.cli.main:app"` (the target the CLI plan fills in). Configure `[tool.hatch.build.targets.wheel] packages = ["src/pyautopsy"]`, `[tool.pytest.ini_options] pythonpath = ["src"]` with `testpaths = ["tests"]`, `[tool.ruff] line-length = 88` with import sorting enabled, and `[tool.mypy]` strict-ish settings (`disallow_untyped_defs`, `warn_unused_ignores`). Create `src/pyautopsy/__init__.py` defining `__version__` as the single source of truth. Create empty-but-present `src/pyautopsy/util/__init__.py`. Write `README.md` documenting native system deps (Fedora `sleuthkit sleuthkit-devel libewf libewf-devel gcc gcc-c++ python3-devel`; Debian/Kali `libtsk-dev libewf-dev build-essential python3-dev`) and the `pip install pyautopsy[ewf]` story for E01. Write a `Containerfile` that bakes `sleuthkit` + `libewf` + the package so raw and E01 work out of the box (per 01-RESEARCH.md §Environment Availability — host lacks libewf-dev). Do NOT add dfVFS, plaso, pyqcow, weasyprint, or jinja2 — explicitly deferred (01-RESEARCH.md §State of the Art).
  </action>
  <verify>
    <automated>python -c "import tomllib,sys; d=tomllib.load(open('pyproject.toml','rb')); assert d['build-system']['build-backend']=='hatchling.build'; assert 'pytsk3==20260520' in d['project']['dependencies']; assert 'ewf' in d['project'].get('optional-dependencies',{}); assert d['tool']['pytest']['ini_options']['pythonpath']==['src']; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` parses; build-backend is `hatchling.build`; `pytsk3==20260520` pinned in dependencies; `ewf` optional-dependency extra present with `libewf-python==20240506`; `pythonpath = ["src"]` set
    - `src/pyautopsy/__init__.py` defines `__version__`
    - `README.md` lists both Fedora and Debian native-dep install commands and the `[ewf]` extra
    - `Containerfile` references `sleuthkit` and `libewf`
    - No deferred heavyweight deps (dfvfs/plaso/pyqcow/weasyprint/jinja2) appear in `pyproject.toml`
  </acceptance_criteria>
  <done>Project installs editable, `import pyautopsy` exposes `__version__`, and `pytest -q` collects from the src layout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UTC-everywhere timestamp helper + ban on naive datetimes (D-10, PITFALLS P4)</name>
  <files>src/pyautopsy/util/timeutil.py, tests/test_timeutil.py</files>
  <read_first>
    - .planning/phases/01-forensic-foundation/01-RESEARCH.md (§Common Pitfalls Pitfall 4; §Code Examples audit writer uses datetime.now(timezone.utc).isoformat())
    - .planning/research/PITFALLS.md (P4 timezone/MAC-time; "ban naive datetime.fromtimestamp")
  </read_first>
  <behavior>
    - Test: `utc_now()` returns a tz-aware datetime whose `tzinfo` is `timezone.utc`
    - Test: `iso_utc()` returns a string ending in `+00:00` (explicit offset, never bare local time)
    - Test: `from_epoch_utc(0)` returns `1970-01-01T00:00:00+00:00` (TSK epoch → tz-aware UTC, never naive `fromtimestamp`)
    - Test: serialized output round-trips through `datetime.fromisoformat` to a tz-aware value
  </behavior>
  <action>
    Create `src/pyautopsy/util/timeutil.py` exporting `utc_now() -> datetime` returning `datetime.now(timezone.utc)`; `iso_utc(dt: datetime | None = None) -> str` returning the ISO-8601 string with explicit `+00:00` offset (default `utc_now()`), raising a specific exception with `raise ... from None` if handed a naive datetime; and `from_epoch_utc(ts: int | float) -> datetime` returning `datetime.fromtimestamp(ts, tz=timezone.utc)` (the sanctioned conversion of TSK epoch times — never bare `fromtimestamp`). This module is the single sanctioned timestamp source per D-10 / PITFALLS P4; all later modules import from here rather than calling `datetime.now()` directly. Use Google-style docstrings and modern type hints per the python-dev skill.
  </action>
  <verify>
    <automated>pytest -q tests/test_timeutil.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q tests/test_timeutil.py` exits 0
    - `utc_now().tzinfo == timezone.utc`
    - `iso_utc()` string ends with `+00:00`
    - `from_epoch_utc(0)` equals a tz-aware `1970-01-01T00:00:00+00:00`
    - passing a naive datetime to `iso_utc` raises a specific (non-bare) exception
  </acceptance_criteria>
  <done>Timestamp helper exists, is tz-aware UTC by construction, and its tests pass — establishing UTC-everywhere from the first phase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test fixtures, conftest, and the failing end-to-end ingest smoke test (Walking Skeleton target)</name>
  <files>tests/__init__.py, tests/conftest.py, tests/fixtures/make_fixtures.py, tests/fixtures/tiny_raw.dd, tests/test_cli_smoke.py</files>
  <read_first>
    - .planning/phases/01-forensic-foundation/01-RESEARCH.md (§Validation Architecture Wave 0 Gaps; §Safe-Extraction Jail malicious-archive fixture strategy; §Open Questions A3 commit a <1 MB raw fixture; §Assumptions A2/A3)
    - .planning/phases/01-forensic-foundation/01-VALIDATION.md (Per-Task Verification Map; Wave 0 Requirements; CLI smoke row)
    - .planning/phases/01-forensic-foundation/01-CONTEXT.md (D-01 case dir layout; D-12 ingest CLI signature)
  </read_first>
  <behavior>
    - `conftest` fixture yields a fresh tmp_path-based case directory per test
    - `conftest` fixture yields the path to the committed tiny raw image; the image is > 0 bytes and reads as raw bytes
    - `make_fixtures.py` deterministically builds: the tiny raw image, a zip-slip tar (`../../escape.txt`), a symlink-escape tar (`link -> /etc/passwd`), a device-file tar, a ratio/size bomb archive, and a count-bomb archive
    - `test_cli_smoke.py` invokes `pyautopsy ingest <tiny_raw> --case <tmp> --examiner X --evidence-id E1` and asserts exit 0 + `case.db` + `logs/audit.jsonl` created — and CURRENTLY FAILS because the command is not yet implemented (xfail/expected-fail marker documenting the Walking Skeleton target)
  </behavior>
  <action>
    Create `tests/__init__.py`. Create `tests/fixtures/make_fixtures.py` with deterministic, programmatic builders (run at build time, not shipped as live bombs): a tiny (<1 MB) raw image written byte-deterministically (committed alongside as `tests/fixtures/tiny_raw.dd` per 01-RESEARCH.md A3 — avoids CI `mkfs`/`mtools` dependency), plus malicious-archive builders using stdlib `tarfile`/`zipfile` for zip-slip, symlink-escape, device-file, ratio/size-bomb, and count-bomb cases (per 01-RESEARCH.md §Safe-Extraction fixture strategy). Commit the generated `tiny_raw.dd`. Create `tests/conftest.py` exposing a `case_dir` fixture (tmp_path subdir) and a `tiny_raw_image` fixture (path to the committed image), plus fixtures that build each malicious archive into tmp_path on demand for the safe-extract plan to consume. Create `tests/test_cli_smoke.py` invoking the `ingest` command (via Typer's `CliRunner` or `subprocess`) per the D-12 signature `pyautopsy ingest <image> --case <case-dir> --examiner <name> --evidence-id <id>`; assert exit 0, `case.db` exists, `logs/audit.jsonl` exists. Mark this test `@pytest.mark.xfail(reason="Walking Skeleton target — ingest command implemented in plan 01-04", strict=True)` so the suite is green now and the marker flips to a real pass once plan 04 lands (executor will remove xfail in plan 04). Do not place fenced implementation code in this plan body.
  </action>
  <verify>
    <automated>pytest -q tests/test_cli_smoke.py && python -c "import os; assert os.path.getsize('tests/fixtures/tiny_raw.dd')>0 and os.path.getsize('tests/fixtures/tiny_raw.dd')<1_000_000; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q tests/test_cli_smoke.py` exits 0 (xfail counts as passing collection; suite green)
    - `tests/fixtures/tiny_raw.dd` exists, is > 0 and < 1 MB
    - `tests/fixtures/make_fixtures.py` defines builders for zip-slip, symlink-escape, device-file, ratio/size-bomb, and count-bomb archives
    - `tests/conftest.py` exposes `case_dir` and `tiny_raw_image` fixtures
    - the smoke test targets the exact D-12 CLI signature and asserts `case.db` + `logs/audit.jsonl`
  </acceptance_criteria>
  <done>Shared fixtures and the failing end-to-end ingest target exist; the suite is green and the Walking-Skeleton finish line is pinned for plans 01-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package-manager install → host | pinned third-party deps (pytsk3/pyewf/typer/rich) introduced at build time |
| committed test fixtures → repo | the tiny raw image and malicious-archive builders live in the repo and feed every downstream security test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-SC | Tampering | pip installs (pytsk3, libewf-python, typer, rich) | mitigate | All 9 deps slopcheck `[OK]` per 01-RESEARCH.md §Package Legitimacy Audit; versions pinned to dated releases in `pyproject.toml`; canonical libyal/TSK/fastapi provenance verified. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages → no blocking checkpoint required. |
| T-1-00-A | Tampering | committed `tiny_raw.dd` fixture | accept | Fixture is a deterministic <1 MB synthetic image generated by `make_fixtures.py`, not real evidence; documented build provenance. Low value, no PII. |
| T-1-00-B | Denial of Service | malicious-archive fixture builders | mitigate | Bombs are generated programmatically at test time (never shipped as live archives) and only ever fed to the jailed `safe_extract` under test (plan 01-03); builders themselves cap generated sizes. |
| T-1-ID | Information Disclosure | tool network behavior | mitigate | No network egress anywhere in scaffold; no telemetry/phone-home dependency added (PITFALLS P11). |
</threat_model>

<verification>
- `pip install -e ".[dev]"` succeeds (raw-only core; `[ewf]` extra documented but not required on this host)
- `ruff check src tests` and `mypy src` clean
- `pytest -q` green: timeutil tests pass, CLI smoke test xfails (strict), fixtures importable
- `tiny_raw.dd` committed and < 1 MB
</verification>

<success_criteria>
- src-layout package installs and imports with `__version__`
- UTC-everywhere helper exists and is tz-aware by construction (D-10)
- Shared fixtures + malicious-archive builders exist for downstream plans
- Failing end-to-end `ingest` smoke test pins the Walking Skeleton target (D-12 signature)
- README + Containerfile document the native-dependency install story (D-13)
</success_criteria>

<output>
Create `.planning/phases/01-forensic-foundation/01-00-SUMMARY.md` when done.
</output>
