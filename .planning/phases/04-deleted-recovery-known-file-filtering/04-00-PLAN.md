---
phase: 04-deleted-recovery-known-file-filtering
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/fixtures/make_fixtures.py
  - tests/fixtures/ext4_orphan.img
  - tests/fixtures/ext4_overwritten.img
  - tests/fixtures/ntfs_resident_deleted.img
  - tests/fixtures/nsrl_minimal.db
  - tests/fixtures/nsrl_metadata.db
  - tests/test_recover.py
  - tests/test_knownfiles.py
autonomous: true
requirements: [RECOV-01, RECOV-02, RECOV-03, FILTER-01]

must_haves:
  truths:
    - "tests/test_recover.py and tests/test_knownfiles.py exist and run, failing with NotImplementedError/import errors (RED), not collection errors"
    - "An ext4 orphan fixture (file + parent dir deleted), an ext4 overwritten fixture (deleted file's blocks reclaimed), and an NTFS resident-deleted fixture are committed with recorded ground-truth constants"
    - "An NSRL-format SQLite fixture (FILE table) and a METADATA-variant copy exist with UPPERCASE-hash rows matching fixture-file hashes"
    - "The committed fixture images and DBs are deterministic (rebuildable byte-identically by re-running make_fixtures.py)"
  artifacts:
    - path: "tests/test_recover.py"
      provides: "Failing RED stubs for RECOV-01/02/03 (recovery, orphan, tier, honesty)"
      contains: "test_recovers_deleted_ext4_content"
    - path: "tests/test_knownfiles.py"
      provides: "Failing RED stubs for FILTER-01 (NSRL membership, custom lists, variant discovery)"
      contains: "test_nsrl_membership"
    - path: "tests/fixtures/make_fixtures.py"
      provides: "Extended fixture builders (orphan/overwritten ext4, resident NTFS, NSRL DBs) + ground-truth constants"
      contains: "build_nsrl_fixture"
    - path: "tests/fixtures/nsrl_minimal.db"
      provides: "NSRL-format FILE-table fixture with UPPERCASE hashes"
  key_links:
    - from: "tests/test_recover.py"
      to: "tests/fixtures/make_fixtures.py"
      via: "imports ground-truth constants (EXT4_ORPHAN_*, NTFS_RESIDENT_*)"
      pattern: "from tests.fixtures.make_fixtures import|import make_fixtures"
    - from: "tests/test_knownfiles.py"
      to: "tests/fixtures/nsrl_minimal.db"
      via: "opens the fixture NSRL DB read-only"
      pattern: "nsrl_minimal|nsrl_metadata"
---

<objective>
Wave 0 scaffold for the Deleted-Recovery & Known-File-Filtering phase: lay down the
RED end-to-end test stubs and the deterministic test fixtures that every later
wave verifies against. This is the failing-test foundation of the MVP vertical
slices — no production code ships here.

Purpose: Establish the Nyquist feedback floor (every RECOV-01/02/03 + FILTER-01
behavior has a named, currently-failing test) and the ground-truth fixtures
(orphan/overwritten ext4, resident-deleted NTFS, NSRL-format SQLite DBs) so the
recovery and filtering slices can be implemented test-first.

Output: tests/test_recover.py, tests/test_knownfiles.py (RED), extended
tests/fixtures/make_fixtures.py, and committed fixture images + NSRL DBs with
recorded ground-truth constants.
</objective>

## Phase Goal

**As a** forensic examiner, **I want to** recover deleted and orphaned files with honest, filesystem-aware confidence labeling and filter the inventory against NSRL and custom hash sets, **so that** I can produce a defensible report that surfaces recoverable evidence and cuts known-file noise without overclaiming.

*(Derived from the ROADMAP Phase 4 goal line, which is outcome-shaped and maps cleanly to the As-a/I-want/so-that slots.)*

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deleted-recovery-known-file-filtering/04-CONTEXT.md
@.planning/phases/04-deleted-recovery-known-file-filtering/04-RESEARCH.md
@.planning/phases/04-deleted-recovery-known-file-filtering/04-PATTERNS.md
@.planning/phases/04-deleted-recovery-known-file-filtering/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend make_fixtures.py with orphan/overwritten ext4, resident NTFS, and NSRL-format SQLite fixtures</name>
  <files>tests/fixtures/make_fixtures.py</files>
  <read_first>
    - tests/fixtures/make_fixtures.py — the file being modified; copy `build_tiny_ext4_image` (lines ~140-180), the `debugfs` request-file pattern (lines ~158-179), the NTFS `ntfscp`-then-delete path (lines ~210-234), `_require_tool` (lines ~121-131), the ground-truth constant block (lines ~97-110: `FS_FILE_NAME`, `EXT4_DELETED_NAME`, `EXT4_DELETED_CONTENT`), and the `main()` builders tuple + committed-image convention (lines ~372-388)
    - .planning/phases/04-deleted-recovery-known-file-filtering/04-RESEARCH.md § "Validation Architecture" → Wave 0 Gaps, § "Environment Availability" (mkfs.ext4/debugfs, mkntfs, sqlite3), Pitfall 2 (ext4 zeroes pointers — use debugfs `rm`/`rmdir` to leave pointers intact), Pitfall 3 (NTFS resident $DATA), Pitfall 4 (NSRL UPPERCASE hashes, FILE vs METADATA table)
    - .planning/phases/04-deleted-recovery-known-file-filtering/04-PATTERNS.md § "tests/fixtures/make_fixtures.py (MODIFIED)"
  </read_first>
  <action>
    Add three image builders and one DB builder, mirroring the existing `build_tiny_ext4_image` shape and `_require_tool` gating, and register each in `main()`'s `builders` tuple so `python tests/fixtures/make_fixtures.py` regenerates them deterministically (committed outputs; CI never runs mkfs).
    (1) `build_ext4_orphan_image(out_path)` — create a file inside a subdirectory, then via a debugfs request file `rm` the file AND `rmdir`/unlink its parent directory so the surviving inode has no parent (orphan, RECOV-02). Record module constants `EXT4_ORPHAN_NAME`, `EXT4_ORPHAN_CONTENT`, and the expected orphan `meta_addr`.
    (2) `build_ext4_overwritten_image(out_path)` — write a file, debugfs `rm` it, then write a new file that reclaims its data blocks; record `EXT4_OVERWRITTEN_DELETED_NAME` and the deleted inode's `meta_addr` whose blocks now overlap an allocated file (drives the `partial/overwritten` tier, RECOV-03/D-31). Use debugfs so ext4 block pointers survive (Pitfall 2) for the intact-vs-overwritten contrast.
    (3) `build_ntfs_resident_deleted_image(out_path)` — create a small NTFS file whose `$DATA` is resident in the MFT record (size < ~700 bytes), then delete it; record `NTFS_RESIDENT_NAME`, `NTFS_RESIDENT_CONTENT`, and the deleted `meta_addr` (resident → no data-block runs, classifies `intact` by MFT-record survival, Pitfall 3).
    (4) `build_nsrl_fixture(minimal_path, metadata_path)` — using stdlib `sqlite3` only (no external tool), build two tiny DBs: one with a `FILE` table and one with a `METADATA` table, both carrying `md5 TEXT, sha1 TEXT, sha256 TEXT` columns and a couple of rows whose hash hex is stored UPPERCASE and equals the UPPERCASE of a known fixture file's hashes (compute them from the committed `tiny_ext4`/`tiny_ntfs` content or a fixed byte string). Record `NSRL_KNOWN_MD5`, `NSRL_KNOWN_SHA1`, `NSRL_KNOWN_SHA256` (lowercase, as our rows store them) and `NSRL_NONMEMBER_MD5`. Write the DBs deterministically (fixed row order; no autoincrement-dependent ordering surprises).
    Do NOT import pytsk3 in this file (fixtures are built with external mkfs/debugfs tools + stdlib sqlite3). Keep every builder pure-deterministic so a rebuild is byte-identical.
  </action>
  <verify>
    <automated>cd /home/oliverh/repos/github/MatrixMagician/PyAutopsy && python tests/fixtures/make_fixtures.py && git status --porcelain tests/fixtures/*.img tests/fixtures/*.db</automated>
  </verify>
  <acceptance_criteria>
    - `python tests/fixtures/make_fixtures.py` exits 0 and produces tests/fixtures/ext4_orphan.img, ext4_overwritten.img, ntfs_resident_deleted.img, nsrl_minimal.db, nsrl_metadata.db
    - Re-running the builder leaves the committed files byte-identical (deterministic; `git status` shows no diff after a second build)
    - Module-level ground-truth constants exist: `EXT4_ORPHAN_NAME/CONTENT`, `EXT4_OVERWRITTEN_DELETED_NAME`, `NTFS_RESIDENT_NAME/CONTENT`, `NSRL_KNOWN_MD5/SHA1/SHA256`, `NSRL_NONMEMBER_MD5`
    - `nsrl_minimal.db` has a `FILE` table; `nsrl_metadata.db` has a `METADATA` table; both store hashes UPPERCASE (verify: `sqlite3 tests/fixtures/nsrl_minimal.db "SELECT md5 FROM FILE LIMIT 1"` returns uppercase hex)
    - make_fixtures.py contains no `import pytsk3`
  </acceptance_criteria>
  <done>All five fixtures committed, deterministic, with ground-truth constants; FILE and METADATA variant DBs present with UPPERCASE hashes.</done>
</task>

<task type="auto">
  <name>Task 2: Write failing RED test stubs in tests/test_recover.py and tests/test_knownfiles.py</name>
  <files>tests/test_recover.py, tests/test_knownfiles.py</files>
  <read_first>
    - tests/test_walk.py — analog for fixture-image integration tests (uses conftest `tiny_ext4_image` fixture, `run_ingest(...)` then the orchestrator, asserts ground-truth constants)
    - tests/test_readonly_guarantee.py — `test_source_unchanged_after_walk` (lines ~37-57), the stat-before/after pattern to mirror for the D-42 case later
    - tests/test_integrity.py — analog for hex/digest unit tests with in-memory values and fakes (for tests/test_knownfiles.py)
    - tests/fixtures/make_fixtures.py — import the ground-truth constants added in Task 1
    - .planning/phases/04-deleted-recovery-known-file-filtering/04-VALIDATION.md § "Per-Task Verification Map" — the exact test names each requirement maps to
    - .planning/phases/04-deleted-recovery-known-file-filtering/04-RESEARCH.md § "Validation Architecture" → "Phase Requirements → Test Map"
  </read_first>
  <action>
    Create the two test files with named stub functions matching the VALIDATION.md map exactly, each currently failing (RED) — import the not-yet-existing modules at call time (inside the test body) or assert against not-yet-implemented behavior so the file COLLECTS cleanly but the tests FAIL rather than erroring at collection.
    In tests/test_recover.py define: `test_recovers_deleted_ext4_content` (RECOV-01: deleted ext4 inode recovers exact `EXT4_DELETED_CONTENT`, cataloged as a `files` row with `allocated=False`, bytes written under a `recovered/` confined tree), `test_recovers_ntfs_resident` (RECOV-01: NTFS resident `$DATA` recovered from MFT), `test_orphan_reported_separately` (RECOV-02: orphan appears in a separate orphan list, not the normal recovered list), `test_confidence_tiers` (RECOV-03: intact vs partial/overwritten vs ext4-zeroed-pointer caveat), `test_no_overclaiming_copy` (RECOV-03/D-32: tier/report copy contains none of a forbidden-substring set — "user deleted", "good", "bad", "malicious", "guilty", "smoking gun" — mirroring the Phase-3 WR-02 honesty test).
    In tests/test_knownfiles.py define: `test_nsrl_membership` (FILTER-01: a known md5 stored UPPERCASE in the fixture DB but lowercase in the row yields a neutral `known{source:nsrl}` annotation; a non-member yields none), `test_custom_hash_sets` (FILTER-01: allow + block lists with comment/blank/mixed-case lines parse; match md5→sha1→sha256; annotation carries source+list+sense, never good/bad), `test_variant_table_discovery` (FILTER-01: both `nsrl_minimal.db` FILE table and `nsrl_metadata.db` METADATA table are discovered and queried).
    Each stub asserts the intended behavior against the (future) `pyautopsy.core.recover` / `pyautopsy.filter.nsrl` / `pyautopsy.filter.hashsets` APIs. Do not implement production code. Do not import pytsk3 in the test files (tests drive through the orchestrator/CLI, not the seam directly).
  </action>
  <verify>
    <automated>cd /home/oliverh/repos/github/MatrixMagician/PyAutopsy && python -m pytest tests/test_recover.py tests/test_knownfiles.py -q --co && python -m pytest tests/test_recover.py tests/test_knownfiles.py -q; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_recover.py tests/test_knownfiles.py --co -q` collects all eight named tests with zero collection errors
    - `python -m pytest tests/test_recover.py tests/test_knownfiles.py -q` exits non-zero (tests are RED — failing, not erroring at import)
    - All test names from 04-VALIDATION.md Per-Task Verification Map are present
    - Neither test file contains `import pytsk3`
    - `python -m pytest tests/test_seam_allowlist.py -x -q` still passes (no new pytsk3 importers introduced)
  </acceptance_criteria>
  <done>Eight named RED tests collect cleanly and fail; seam-allowlist gate stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build tools → fixture files | mkfs/debugfs/sqlite3 generate committed binary fixtures (build-time only, trusted host) |
| (no runtime evidence boundary in this wave) | Wave 0 ships no code that reads evidence; threats are modeled in the recovery/filtering waves |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-00-DET | Tampering | committed fixtures | mitigate | Builders are deterministic; a rebuild is byte-identical (verified via `git status` after re-run) so a fixture cannot silently drift |
| T-04-00-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this wave (research Package Legitimacy Audit: none); fixtures use already-pinned tooling + stdlib sqlite3 |
</threat_model>

<verification>
- `python tests/fixtures/make_fixtures.py` regenerates all five fixtures deterministically
- `python -m pytest tests/test_recover.py tests/test_knownfiles.py --co -q` collects 8 tests cleanly
- `python -m pytest tests/test_recover.py tests/test_knownfiles.py -q` is RED (expected)
- `python -m pytest tests/test_seam_allowlist.py -x -q` is GREEN
</verification>

<success_criteria>
- Five deterministic fixtures committed with ground-truth constants
- Eight named RED tests collect cleanly and fail (not error)
- No new pytsk3 importers; seam-allowlist gate green
</success_criteria>

<output>
Create `.planning/phases/04-deleted-recovery-known-file-filtering/04-00-SUMMARY.md` when done
</output>
