---
phase: 04-deleted-recovery-known-file-filtering
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - src/pyautopsy/core/analyze.py
  - src/pyautopsy/cli/main.py
  - src/pyautopsy/report/assemble.py
  - tests/test_reproducibility.py
  - tests/test_readonly_guarantee.py
autonomous: true
requirements: [RECOV-01, RECOV-02, RECOV-03, FILTER-01]

must_haves:
  truths:
    - "analyze runs recovery only when recover input is supplied and filtering only when --nsrl/--hash-set is supplied; default analyze (no inputs) report stays byte-identical to the Phase-3 baseline (D-40)"
    - "Two analyze --recover --nsrl runs on the same fixture produce byte-identical report.json and identical recovered/ filenames (CLI-02/D-41)"
    - "The evidence source bytes are unchanged after recover and the Phase-1 end-of-run re-verify still runs (D-42)"
    - "The seam allowlist stays green — recover/filter modules import no pytsk3 (D-14)"
    - "report/assemble.py _MVP_LIMITATIONS no longer asserts the report excludes deleted-file recovery / NSRL filtering WHEN those features run, while the default-unchanged determinism test still holds (D-28/D-32)"
  artifacts:
    - path: "src/pyautopsy/core/analyze.py"
      provides: "Opt-in recover + filter steps spliced into run_analyze, gated on supplied inputs"
      contains: "run_recover"
    - path: "src/pyautopsy/cli/main.py"
      provides: "analyze --recover / --nsrl / --hash-set flags + recover --nsrl/--hash-set flags"
      contains: "nsrl"
    - path: "tests/test_reproducibility.py"
      provides: "test_recover_filter_reproducible + test_default_analyze_unchanged"
      contains: "test_default_analyze_unchanged"
    - path: "tests/test_readonly_guarantee.py"
      provides: "test_recover_does_not_write_source"
      contains: "test_recover_does_not_write_source"
  key_links:
    - from: "src/pyautopsy/core/analyze.py"
      to: "src/pyautopsy/core/recover.py"
      via: "run_recover called when recover input supplied"
      pattern: "run_recover"
    - from: "src/pyautopsy/core/analyze.py"
      to: "src/pyautopsy/core/knownfiles.py"
      via: "run_filter called when --nsrl/--hash-set supplied"
      pattern: "run_filter"
    - from: "tests/test_reproducibility.py"
      to: "report.json"
      via: "byte-compare two runs + recovered/ filename compare"
      pattern: "report.json|recovered"
---

<objective>
The INTEGRATION slice: wire recovery + filtering into the `analyze` pipeline as
opt-in steps (D-40), prove determinism / read-only / seam invariants with
extended tests, and update the report's honesty disclaimer so it stays truthful
once recovery+filtering run.

Purpose: Close the phase end-to-end — a single `pyautopsy analyze <image>
--case <dir> --recover --nsrl <db> --hash-set <list>` produces the full report
with recovered/orphan/known sections, while the default `analyze` stays exactly
Phase-3 byte-identical. Lock CLI-02/D-40/D-41/D-42/D-14 with the reproducibility,
read-only, and seam-allowlist gates.

Output: opt-in analyze wiring + the remaining CLI flags, the
reproducibility/readonly tests, and the corrected _MVP_LIMITATIONS copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deleted-recovery-known-file-filtering/04-CONTEXT.md
@.planning/phases/04-deleted-recovery-known-file-filtering/04-RESEARCH.md
@.planning/phases/04-deleted-recovery-known-file-filtering/04-PATTERNS.md
@.planning/phases/04-deleted-recovery-known-file-filtering/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Opt-in recover + filter wiring in run_analyze + the remaining CLI flags + _MVP_LIMITATIONS honesty update</name>
  <files>src/pyautopsy/core/analyze.py, src/pyautopsy/cli/main.py, src/pyautopsy/report/assemble.py</files>
  <read_first>
    - src/pyautopsy/core/analyze.py — `run_analyze` (lines ~154-317); opt-in step insertion point BETWEEN run_walk (line ~236) and build_timeline/report (lines ~247-260); `store = CaseStore.open(case_path)` (line ~246); threading `acquisition_verified` into assemble_report_body (lines ~248-256); `AnalyzeResult` + audit.end fields (lines ~101-129, 273-281)
    - src/pyautopsy/cli/main.py — the `analyze` command (lines ~209-306) and the `recover` command added in 04-01; `--max-hash-size` Annotated option shape (lines ~163-169); `_INTEGRITY_EXIT_CODE` (line ~41)
    - src/pyautopsy/report/assemble.py — `_MVP_LIMITATIONS` (lines ~38-43) which currently states the report does NOT include deleted-file recovery / NSRL filtering; the deterministic body contract (lines ~1-23, 245-253)
    - src/pyautopsy/core/recover.py (run_recover) + src/pyautopsy/core/knownfiles.py (run_filter) from 04-01/04-02
    - .planning/phases/04-deleted-recovery-known-file-filtering/04-CONTEXT.md D-40 (opt-in gating), D-41 (determinism)
    - .planning/phases/04-deleted-recovery-known-file-filtering/04-PATTERNS.md § core/analyze.py + § report/assemble.py (_MVP_LIMITATIONS note)
  </read_first>
  <action>
    In core/analyze.py: splice recovery + filtering into `run_analyze` BETWEEN the walk and the timeline/report build, running each ONLY when its input is supplied (D-40): call `run_recover(...)` when a recover flag is set, and `run_filter(case_path, nsrl_db=..., hash_sets=...)` when `--nsrl`/`--hash-set` is supplied. Thread the real RecoverResult/FilterResult counts into `assemble_report_body` the same honest way `acquisition_verified` is threaded, and extend `AnalyzeResult` + the `analyze.end` audit event with the recovered/known counts (analytical content only, no wall-clock). When NO recover/filter inputs are supplied, the pipeline must be unchanged from Phase 3 so the body stays byte-identical.
    In cli/main.py: add `--recover` (bool), `--nsrl` (`Path | None`, `typer.Option(exists=True, dir_okay=False, readable=True)`), and `--hash-set` (repeatable, each carrying an allow/block sense — e.g. `list[str] | None` parsed as `path:sense` or paired `--hash-set-allow`/`--hash-set-block` options) on BOTH the `analyze` command and the `recover` command, following the `--max-hash-size` Annotated shape. Validate the supplied paths before work (Typer `exists=True` covers most). Pass them through to run_analyze/run_recover/run_filter.
    In report/assemble.py: update `_MVP_LIMITATIONS` so it no longer flatly states the report excludes deleted-file recovery / NSRL filtering. Make the disclaimer conditional/honest — when recovery/filtering ran, the limitations text reflects what DID run (and its caveats: ext4 pointer-zeroing, carving deferred = CARVE-01, NSRL is examiner-supplied noise-reduction not adjudication); when they did NOT run, the default text stays exactly as Phase 3 shipped so `test_default_analyze_unchanged` holds. No intent/good-bad language (D-32).
  </action>
  <verify>
    <automated>cd /home/oliverh/repos/github/MatrixMagician/PyAutopsy && python -m pytest tests/test_seam_allowlist.py -x -q && python -m pytest -q -k "analyze or recover or report or cli"</automated>
  </verify>
  <acceptance_criteria>
    - `pyautopsy analyze --help` shows --recover, --nsrl, --hash-set; `pyautopsy recover --help` shows --nsrl, --hash-set
    - With no recover/filter inputs, run_analyze produces the same report body as before (verified by test_default_analyze_unchanged in Task 2)
    - With inputs supplied, the report includes recovered/orphan/known sections and AnalyzeResult carries the counts
    - `_MVP_LIMITATIONS` no longer asserts recovery/NSRL are excluded when they ran; default text unchanged when they did not
    - `python -m pytest tests/test_seam_allowlist.py -x -q` passes; `ruff check` + `mypy` clean on the modified files
  </acceptance_criteria>
  <done>analyze gains opt-in recover/filter steps + flags; default analyze unchanged; honesty disclaimer corrected.</done>
</task>

<task type="auto">
  <name>Task 2: Reproducibility + read-only integration tests (CLI-02/D-40/D-41/D-42)</name>
  <files>tests/test_reproducibility.py, tests/test_readonly_guarantee.py</files>
  <read_first>
    - tests/test_reproducibility.py — `_analyze` runner (lines ~49-69) and `test_two_analyze_runs_byte_identical_report` (lines ~147-188) to copy for the recover/filter variant + the default-unchanged case
    - tests/test_readonly_guarantee.py — `test_source_unchanged_after_walk` (lines ~37-57): stat-before/after (`st_mtime_ns`, `st_size`) pattern to mirror for recover
    - tests/fixtures/make_fixtures.py — fixtures + nsrl_minimal.db from Wave 0
    - .planning/phases/04-deleted-recovery-known-file-filtering/04-VALIDATION.md § Per-Task Verification Map rows 4-int-01/02/04
    - .planning/phases/04-deleted-recovery-known-file-filtering/04-RESEARCH.md Pitfall 6 (determinism — inode order + recovered-name collisions), D-41/D-42
    - .planning/phases/04-deleted-recovery-known-file-filtering/04-PATTERNS.md § test_reproducibility.py + test_readonly_guarantee.py
  </read_first>
  <action>
    In tests/test_reproducibility.py: copy the `_analyze` helper to add `--recover`/`--nsrl <fixture-db>`/`--hash-set` args. Add `test_recover_filter_reproducible` — run the recover+filter analyze twice into `case_a`/`case_b`, assert `report.json` whole-file byte-equal AND the set of `recovered/` filenames identical across runs (D-41/Pitfall 6). Add `test_default_analyze_unchanged` — run a plain `analyze` (no recover/filter inputs) and assert its `report.json`/`report.html` byte-equal the Phase-3 baseline (capture the baseline by running plain analyze twice, or compare against the existing Phase-3 reproducibility expectation), proving D-40 left the default path untouched.
    In tests/test_readonly_guarantee.py: copy `test_source_unchanged_after_walk` into `test_recover_does_not_write_source` — stat the source image before, run `run_ingest` + `run_recover` (and a filter pass), stat after, assert `st_mtime_ns` and `st_size` unchanged, and that the Phase-1 end-of-run re-verify still runs (D-42).
    Use the Wave-0 fixtures + NSRL DB. These tests must be deterministic (no wall-clock assertions).
  </action>
  <verify>
    <automated>cd /home/oliverh/repos/github/MatrixMagician/PyAutopsy && python -m pytest tests/test_reproducibility.py tests/test_readonly_guarantee.py -x -q && python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_reproducibility.py::test_recover_filter_reproducible -x -q` passes (byte-identical report.json + identical recovered/ filenames across two runs)
    - `python -m pytest tests/test_reproducibility.py::test_default_analyze_unchanged -x -q` passes (default analyze byte-identical to Phase-3 baseline — D-40)
    - `python -m pytest tests/test_readonly_guarantee.py::test_recover_does_not_write_source -x -q` passes (source unchanged; re-verify runs — D-42)
    - `python -m pytest -q` full suite GREEN; `python -m pytest tests/test_seam_allowlist.py -x -q` GREEN
    - `ruff check` + `mypy` clean
  </acceptance_criteria>
  <done>Reproducibility (recover+filter and default-unchanged) and read-only-after-recover gates all pass; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags (--nsrl/--hash-set paths) → pipeline | Untrusted file paths validated before work (Typer exists=True) |
| analyze pipeline → evidence source | Recovery reads are read-only; only the case dir is written |
| default analyze → report body | Must remain byte-identical to Phase 3 (no regression surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-RO | Tampering (forensic soundness) | recover step in analyze writing the source | mitigate | run_recover re-asserts assert_source_not_mounted; only case dir written; test_recover_does_not_write_source + Phase-1 re-verify lock it (D-42) |
| T-04-03-DET | Repudiation | non-deterministic recovered names / report drift breaking CLI-02 | mitigate | Deterministic vol/off/meta_addr naming + store total order; test_recover_filter_reproducible + test_default_analyze_unchanged enforce byte-stability (D-41/D-40) |
| T-04-03-DISC | Repudiation | stale _MVP_LIMITATIONS overclaiming/underclaiming what ran | mitigate | Conditional, honest limitations copy reflecting what actually ran; default text unchanged; honesty test green (D-28/D-32) |
| T-04-03-SEAM | Tampering | a recover/filter import leaking pytsk3 outside the seam | mitigate | test_seam_allowlist.py gate run in both tasks |
| T-04-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages (research audit: none) |
</threat_model>

<verification>
- `python -m pytest tests/test_reproducibility.py tests/test_readonly_guarantee.py -x -q` GREEN
- `python -m pytest tests/test_seam_allowlist.py -x -q` GREEN
- `python -m pytest -q` full suite GREEN
- `ruff check` + `mypy` clean
- Manual (UAT, from 04-VALIDATION.md): open reports/report.html, confirm separate Recovered/Orphan/Known sections, tier glyphs (not color-only), no intent/good-bad language
</verification>

<success_criteria>
- One `analyze --recover --nsrl <db> --hash-set <list>` produces the full report with recovered/orphan/known sections, deterministically
- Default `analyze` stays Phase-3 byte-identical (D-40)
- Read-only, determinism, seam-allowlist, and honesty invariants all locked by passing gates
</success_criteria>

<output>
Create `.planning/phases/04-deleted-recovery-known-file-filtering/04-03-SUMMARY.md` when done
</output>
