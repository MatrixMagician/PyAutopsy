---
phase: 02-filesystem-walk-metadata
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - src/pyautopsy/evidence/filetype.py
  - tests/fixtures/make_fixtures.py
  - tests/conftest.py
  - tests/fixtures/tiny_ext4.img
  - tests/fixtures/tiny_ntfs.img
  - tests/fixtures/tiny_fat32.img
  - tests/fixtures/tiny_partitioned.img
  - tests/test_seam_allowlist.py
  - tests/test_filesystem.py
  - tests/test_walk.py
  - tests/test_readonly_guarantee.py
autonomous: true
requirements: [META-01, META-02, META-03, META-04, META-05]
must_haves:
  truths:
    - "Tiny committed ext4/NTFS/FAT32/partitioned fixture images exist and load without a CI mkfs dependency"
    - "tests/test_seam_allowlist.py is an executable test that fails if pytsk3/pyewf is imported outside the D-14 allowlist"
    - "python-magic==0.4.27 is a declared dependency and filetype.py refuses the wrong magic binding at import"
    - "Failing test stubs exist for META-01..05, D-20, and read-only-after-walk (the Nyquist scaffold)"
  artifacts:
    - path: "src/pyautopsy/evidence/filetype.py"
      provides: "python-magic binding-collision guard + from_buffer content typing wrapper"
      contains: "from_buffer"
    - path: "tests/test_seam_allowlist.py"
      provides: "executable D-14 seam allowlist arch guard"
    - path: "tests/fixtures/make_fixtures.py"
      provides: "ext4/NTFS/FAT32/partitioned fixture builders"
      contains: "build_tiny_ext4_image"
    - path: "tests/test_walk.py"
      provides: "failing META-01..05 + D-20 orchestrator test stubs"
    - path: "pyproject.toml"
      provides: "python-magic==0.4.27 dependency"
      contains: "python-magic"
  key_links:
    - from: "src/pyautopsy/evidence/filetype.py"
      to: "magic.from_buffer"
      via: "hasattr guard at import then from_buffer call"
      pattern: "hasattr\\(magic, .from_buffer.\\)"
    - from: "tests/test_seam_allowlist.py"
      to: "src/pyautopsy"
      via: "file walk + regex over *.py asserting only image.py + filesystem.py import pytsk3"
      pattern: "import pytsk3"
---

<objective>
Wave 0 scaffold for Phase 2: make the validation infrastructure exist and fail
RED before any walk implementation, per 02-VALIDATION.md. Add the new native
system dependency (`python-magic`) and its binding-collision guard, build and
commit the tiny ext4/NTFS/FAT32/partitioned fixture images, turn the D-14 seam
rule into an EXECUTABLE test (it is only a convention today), and lay down
failing test stubs covering META-01..05 + D-20 + read-only-after-walk.

Purpose: Establish the Nyquist feedback loop and the test-first scaffold so the
implementation slices (Plans 01-03) have green-able targets and a fast (~15s)
sampling command. Closes the 02-VALIDATION.md Wave 0 gaps and makes
`nyquist_compliant: true` achievable.
Output: New `filetype.py` (with import guard), four committed fixture images,
fixture builders + conftest fixtures, `test_seam_allowlist.py`, failing
`test_filesystem.py` / `test_walk.py` stubs, an extended
`test_readonly_guarantee.py`, and `python-magic==0.4.27` in pyproject.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-filesystem-walk-metadata/02-CONTEXT.md
@.planning/phases/02-filesystem-walk-metadata/02-RESEARCH.md
@.planning/phases/02-filesystem-walk-metadata/02-PATTERNS.md
@.planning/phases/02-filesystem-walk-metadata/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add python-magic dependency and the filetype.py binding-collision guard</name>
  <files>pyproject.toml, src/pyautopsy/evidence/filetype.py</files>
  <read_first>
    - pyproject.toml (lines 26-36: `[project.dependencies]` = pytsk3/typer/rich, `[project.optional-dependencies]`)
    - src/pyautopsy/evidence/image.py (lines 1-23 seam docstring; lines 233-263 `_open_ewf` lazy-import + actionable-error guard — the analog for filetype.py per 02-PATTERNS.md)
    - 02-RESEARCH.md Pitfall 1 (lines 383-390) + Pattern 6 (lines 316-329) — the from_buffer typing core
    - 02-PATTERNS.md §`evidence/filetype.py` (lines 56-71)
  </read_first>
  <action>
    Add `"python-magic==0.4.27"` to `[project.dependencies]` in pyproject.toml (alongside pytsk3==20260520 per D-19; libmagic is the system dep documented in research). Create `src/pyautopsy/evidence/filetype.py` as the isolated python-magic wrapper (D-19, NOT a pytsk3 seam — it must NOT import pytsk3). Module docstring cites D-19 + Pitfall 1 in the image.py tone. At module level `import magic` then assert the binding with `hasattr(magic, "from_buffer")`; if absent raise `ImportError` with the actionable message "the installed 'magic' module is file-magic, not python-magic; install python-magic==0.4.27 (the two collide on the 'magic' name)." (mirror `ImageOpenError`'s install-hint style). Expose a `file_type(read_head, size)` function taking a head-reader callable + size: size 0 → return `"inode/x-empty"`; else read up to `HEAD_BYTES = 4096` leading bytes via the callable and return `magic.from_buffer(head, mime=True)`; empty read → `None`. Keep `magic` isolated here so the orchestrator stays testable with a fake. After editing pyproject, run `pip install -e .` so the new import resolves and ensure `file-magic` is not shadowing (`pip uninstall -y file-magic` if present).
  </action>
  <verify>
    <automated>python3 -c "import magic, sys; sys.exit(0 if hasattr(magic, 'from_buffer') else 1)" && python3 -c "from pyautopsy.evidence import filetype; assert filetype.file_type(lambda o,s: b'', 0) == 'inode/x-empty'"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "from pyautopsy.evidence import filetype"` imports without raising (correct binding present).
    - Source assertion: `grep -n 'hasattr(magic, "from_buffer")' src/pyautopsy/evidence/filetype.py` matches.
    - Source assertion: `grep -c 'import pytsk3' src/pyautopsy/evidence/filetype.py` is 0 (filetype.py is NOT a pytsk3 seam).
    - `grep -c 'python-magic==0.4.27' pyproject.toml` is >= 1.
  </acceptance_criteria>
  <done>python-magic==0.4.27 declared and installed; filetype.py imports, guards the binding, and types an empty buffer as inode/x-empty.</done>
</task>

<task type="auto">
  <name>Task 2: Build and commit tiny ext4/NTFS/FAT32/partitioned fixture images + conftest fixtures</name>
  <files>tests/fixtures/make_fixtures.py, tests/conftest.py, tests/fixtures/tiny_ext4.img, tests/fixtures/tiny_ntfs.img, tests/fixtures/tiny_fat32.img, tests/fixtures/tiny_partitioned.img</files>
  <read_first>
    - tests/fixtures/make_fixtures.py (lines 1-67 tiny-raw deterministic builder + module docstring; lines 146-154 `main()` regeneration entrypoint)
    - tests/conftest.py (lines 22-48: FIXTURES_DIR + `tiny_raw_image` committed-fixture fixture — the pattern to mirror)
    - 02-RESEARCH.md Environment Availability (lines 474-487: mkfs.ext4+debugfs, mkfs.fat+mtools with `MTOOLS_SKIP_CHECK=1 -F 32` ≥64MB, mkntfs -F -Q) + Wave 0 Gaps (lines 523-529) + Open Question 1 (lines 462-466: partitioned image via parted/sfdisk + 2 FS)
    - 02-VALIDATION.md Wave 0 Requirements (line 61)
  </read_first>
  <action>
    Extend `tests/fixtures/make_fixtures.py` with `build_tiny_ext4_image(dest)` (`mkfs.ext4 -F` on a small file, then `debugfs -w` to write a known regular file `/file1.txt` with known content + uid/gid/mode, and a second file that is then `rm`'d to leave a DELETED entry for the META-01 deleted-inventory test), `build_tiny_fat32_image(dest)` (`mkfs.fat -F 32` on a ≥64MB file, then `MTOOLS_SKIP_CHECK=1 mcopy` a known file — FAT for the local-time D-16 test), `build_tiny_ntfs_image(dest)` (`mkntfs -F -Q`), and `build_partitioned_image(dest)` (parted/sfdisk creating a partition table with a FAT and an ext4 partition for the D-15 volume-offset test, Open Question A1). Each builder writes deterministically and, like `build_tiny_raw_image`, the BUILT images are committed under tests/fixtures/ so CI needs no mkfs (mirror the tiny_raw.dd approach). Extend `main()` to regenerate all four next to the module. Run `python3 tests/fixtures/make_fixtures.py` to produce `tiny_ext4.img`, `tiny_ntfs.img`, `tiny_fat32.img`, `tiny_partitioned.img` and commit them. Add conftest fixtures `tiny_ext4_image`, `tiny_ntfs_image`, `tiny_fat32_image`, `tiny_partitioned_image` mirroring `tiny_raw_image` (assert the committed file exists). Record the exact ground-truth row count + the known deleted/uid/gid/mode values as module constants so the META tests can assert exact counts (the Nyquist "every file" signal).
  </action>
  <verify>
    <automated>python3 -c "from tests.fixtures import make_fixtures as m; import pathlib; d=pathlib.Path('tests/fixtures'); assert all((d/n).is_file() and (d/n).stat().st_size>0 for n in ('tiny_ext4.img','tiny_ntfs.img','tiny_fat32.img','tiny_partitioned.img'))"</automated>
  </verify>
  <acceptance_criteria>
    - All four images exist as committed >0-byte files under tests/fixtures/.
    - Source assertion: `grep -c 'def build_tiny_ext4_image' tests/fixtures/make_fixtures.py` is >= 1; same for fat32, ntfs, partitioned builders.
    - Source assertion: conftest exposes the four new `*_image` fixtures (`grep -c 'def tiny_ext4_image' tests/conftest.py` >= 1).
    - Opening each image with the Phase 1 seam `open_image(...)` succeeds (the byte layer is readable).
  </acceptance_criteria>
  <done>Four tiny filesystem fixture images committed; builders + conftest fixtures present; ground-truth counts recorded for exact-count assertions.</done>
</task>

<task type="auto">
  <name>Task 3: Executable D-14 seam allowlist test + failing META/D-20/read-only test stubs</name>
  <files>tests/test_seam_allowlist.py, tests/test_filesystem.py, tests/test_walk.py, tests/test_readonly_guarantee.py</files>
  <read_first>
    - tests/test_image.py (seam-test style: module docstring citing requirement, public-surface imports, try/finally close, message-substring assertions)
    - tests/test_readonly_guarantee.py (lines 24-34: `test_source_mtime_and_size_unchanged_after_open_and_hash` — the analog to mirror for the walk)
    - 02-RESEARCH.md Validation Architecture → Phase Requirements → Test Map (lines 500-511) — the exact pytest node IDs each stub must define
    - 02-PATTERNS.md §`tests/test_seam_allowlist.py` (lines 219-221: no analog; scan src/ for `import pytsk3`/`import pyewf`, assert importers are EXACTLY {evidence/image.py, evidence/filesystem.py})
  </read_first>
  <action>
    Create `tests/test_seam_allowlist.py`: walk `src/pyautopsy/**/*.py`, regex each file for a top-level `import pytsk3` / `import pyewf` / `from pytsk3` / `from pyewf` (ignore `from __future__`), and assert the set of importing files equals EXACTLY the D-14 allowlist `{src/pyautopsy/evidence/image.py, src/pyautopsy/evidence/filesystem.py}`. Make it executable now: filesystem.py does not exist yet, so the allowlist membership for it is "permitted-if-present" — assert NO file OUTSIDE the allowlist imports pytsk3/pyewf (this passes today with only image.py, and stays green once filesystem.py is added in Plan 01). Create `tests/test_filesystem.py` with failing stubs `test_volume_enumeration`, `test_bare_fs_fallback`, `test_fs_type_detection`, `test_recursion_correctness` (each `import pytest; pytest.fail("Plan 01")` or referencing not-yet-existing `evidence.filesystem`). Create `tests/test_walk.py` with failing stubs for the exact node IDs from the test map: `test_inventory_includes_deleted_entry`, `test_macb_utc_and_fat_flagged`, `test_no_naive_datetimes`, `test_ownership_and_mode`, `test_three_digest_single_pass`, `test_filetype_by_content_not_extension`, `test_unsupported_volume_records_limitation` (each xfail/fail referencing `core.walk` which does not exist yet). Extend `tests/test_readonly_guarantee.py` with `test_source_unchanged_after_walk` (stub failing now; will stat before/after a full walk over the fs fixtures and assert `st_mtime_ns`+`st_size` unchanged, mirroring the existing open+hash test). Each stub carries the requirement ID in its docstring. Stubs MUST be collected (not skipped) and MUST be RED.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_seam_allowlist.py -x -q && python3 -m pytest tests/test_walk.py tests/test_filesystem.py -q 2>&1 | grep -Eq 'fail|error'</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_seam_allowlist.py -x` exits 0 today (only image.py imports pytsk3 — verified by grep at planning time).
    - `pytest tests/test_walk.py tests/test_filesystem.py` reports failures/errors (RED) for every named node ID, none skipped.
    - Source assertion: `grep -c 'def test_inventory_includes_deleted_entry' tests/test_walk.py` is 1; likewise for `test_macb_utc_and_fat_flagged`, `test_no_naive_datetimes`, `test_ownership_and_mode`, `test_three_digest_single_pass`, `test_filetype_by_content_not_extension`, `test_unsupported_volume_records_limitation`.
    - Source assertion: `grep -c 'def test_source_unchanged_after_walk' tests/test_readonly_guarantee.py` is 1.
  </acceptance_criteria>
  <done>Seam allowlist test passes (executable gate, not a convention); all META-01..05 + D-20 + read-only-after-walk stubs exist and are RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk image → walk | The evidence image is untrusted input; this plan only builds *trusted* fixtures, but the stubs encode the threats the implementation must mitigate. |
| `magic` import | Wrong binding (file-magic vs python-magic) is a supply/runtime hazard crossing into typing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-00-MAGIC | Tampering | `evidence/filetype.py` `import magic` | mitigate | `hasattr(magic, "from_buffer")` guard at import raises an actionable ImportError if file-magic is shadowing python-magic (Pitfall 1, this Task 1). |
| T-2-00-SC | Tampering | `pip install python-magic` | accept | python-magic rated slopcheck `[OK]`, millions of downloads, github.com/ahupp/python-magic (02-RESEARCH Package Legitimacy Audit). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages → no blocking checkpoint required. |
| T-2-00-FIX | Tampering | fixture builders | accept | Fixtures are deterministic, built once, committed; builders cap their own size (mirror T-1-00-B). No live bombs shipped. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_seam_allowlist.py -x` exits 0.
- `python3 -m pytest tests/test_walk.py tests/test_filesystem.py` is RED (scaffold, pre-implementation).
- `python3 -c "from pyautopsy.evidence import filetype"` imports cleanly.
- All four fixture images committed and openable via the Phase 1 seam.
</verification>

<success_criteria>
- python-magic==0.4.27 declared + installed; filetype.py binding guard in place.
- Four tiny FS fixture images committed; builders + conftest fixtures present.
- test_seam_allowlist.py is an executable D-14 gate (green now).
- META-01..05 + D-20 + read-only-after-walk stubs exist and are RED.
</success_criteria>

<output>
Create `.planning/phases/02-filesystem-walk-metadata/02-00-SUMMARY.md` when done
</output>
