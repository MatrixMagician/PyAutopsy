---
phase: 02-filesystem-walk-metadata
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/pyautopsy/core/walk.py
autonomous: true
requirements: [META-02, META-03]
must_haves:
  truths:
    - "Each file row records MACB timestamps as tz-aware UTC ISO-8601 with an explicit +00:00 offset"
    - "FAT timestamps are rebased via the --timezone option and flagged local-time-inferred with assumed_timezone in attributes; ext4/NTFS are treated as UTC"
    - "A zero TSK timestamp is stored as None (never a fake 1970-01-01)"
    - "No naive datetime can be persisted — every MACB value is routed through iso_utc which rejects naive datetimes"
    - "timestamp_source records fs-type + originating attribute (ext4:inode / ntfs:$STANDARD_INFORMATION / fat:dir-entry)"
    - "Each file row records ownership UID/GID and permission/mode bits"
  artifacts:
    - path: "src/pyautopsy/core/walk.py"
      provides: "MACB→UTC normalization (FAT local-time handling) + uid/gid/mode population per FileRow"
      contains: "timestamp_source"
  key_links:
    - from: "src/pyautopsy/core/walk.py"
      to: "src/pyautopsy/util/timeutil.py"
      via: "from_epoch_utc + iso_utc for every MACB value"
      pattern: "iso_utc\\("
    - from: "src/pyautopsy/core/walk.py"
      to: "FileRow.mtime_utc/atime_utc/ctime_utc/crtime_utc"
      via: "normalized MACB strings written into the FileRow"
      pattern: "crtime_utc"
---

<objective>
Enrich each inventoried file row with forensically-correct time and ownership
metadata: MACB timestamps normalized to tz-aware UTC ISO-8601 with explicit
offset (META-02), with FAT local-time handling + flagging (D-16), zero→None
(Pitfall 3), and the no-naive-datetimes invariant structurally enforced; plus
UID/GID and mode bits per file (META-03).

Purpose: The timeline (Phase 3) is only defensible if every timestamp is UTC,
offset-explicit, and honest about FAT's local-time imprecision. This slice makes
the inventory time-correct without changing the walk shape from Plan 01.
Output: The MACB columns (`mtime_utc`/`atime_utc`/`ctime_utc`/`crtime_utc`),
`timestamp_source`, and `uid`/`gid`/`mode` columns — null after Plan 01 — are now
populated by `core/walk.py`, with FAT flags in `attributes`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-filesystem-walk-metadata/02-RESEARCH.md
@.planning/phases/02-filesystem-walk-metadata/02-PATTERNS.md
@src/pyautopsy/util/timeutil.py
@src/pyautopsy/core/walk.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MACB → tz-aware UTC ISO-8601 with FAT local-time handling and zero→None</name>
  <files>src/pyautopsy/core/walk.py</files>
  <read_first>
    - src/pyautopsy/util/timeutil.py (full file: `from_epoch_utc` lines 57-70, `iso_utc` lines 30-54 — `iso_utc` REJECTS naive datetimes, the structural invariant)
    - src/pyautopsy/core/walk.py (the Plan 01 per-entry loop building FileRow — the insertion point for MACB fields)
    - 02-RESEARCH.md Pattern 4 (lines 254-285: `macb_iso` + FAT_TYPES set + `timestamp_source` per fs type + `local-time-inferred`/`assumed_timezone` attrs; M=mtime A=atime C=ctime B=crtime) + Pitfall 2 (lines 392-395 FAT local-time) + Pitfall 3 (lines 397-400 zero→None)
    - 02-PATTERNS.md Shared Patterns §UTC-everywhere (lines 257-266)
  </read_first>
  <behavior>
    - ext4/NTFS entry with epoch mtime → FileRow.mtime_utc is an ISO-8601 string ending in "+00:00" equal to `iso_utc(from_epoch_utc(secs))`.
    - FAT entry → MACB value rebased through `ZoneInfo(timezone)` then expressed as UTC; `attributes["time_precision"] == "local-time-inferred"` and `attributes["assumed_timezone"]` set to the walk timezone.
    - A meta field of 0 (e.g. ext2 crtime, or unrecorded) → the corresponding `*_utc` column is None (never 1970-01-01).
    - `timestamp_source` holds the EXACT per-fs string derived from `FileEntry.fs_ftype`: the ext4 fixture's rows yield `ext4:inode`, the NTFS fixture's `ntfs:$STANDARD_INFORMATION`, and the FAT fixture's `fat:dir-entry` (asserted against all three fixtures, not just presence).
    - Every persisted MACB value re-parses to a timezone-aware datetime (no naive datetimes anywhere).
  </behavior>
  <action>
    In `core/walk.py`, add a MACB-normalization helper following RESEARCH Pattern 4: classify FAT by importing `FAT_FS_TYPES` (the plain-int `frozenset[int]` exported from `evidence/filesystem.py` in Plan 02-01 Task 2) and testing `FileEntry.fs_ftype in FAT_FS_TYPES` (walk.py must NOT import pytsk3 — D-14; `fs_ftype` is already the plain int carried on `FileEntry`, and `FAT_FS_TYPES` is the seam's defined pytsk3-free symbol — do NOT reference raw `pytsk3.TSK_FS_TYPE_FAT*` constants here). For ext4/NTFS pass each non-zero epoch through `from_epoch_utc(secs)` then `iso_utc(...)`; for FAT build `datetime.fromtimestamp(secs, tz=ZoneInfo(timezone))` then `iso_utc(...)` (which converts aware→UTC) and set `attributes["time_precision"]="local-time-inferred"` + `attributes["assumed_timezone"]=str(walk_tz)`. A meta epoch of `0` → `None` (Pitfall 3). Map M=mtime, A=atime, C=ctime, B=crtime into `FileRow.mtime_utc/atime_utc/ctime_utc/crtime_utc`. Set `timestamp_source` from `fs_ftype` (`ext4:inode`/`ntfs:$STANDARD_INFORMATION`/`fat:dir-entry`). Fold `*_nano` sub-second values into `attributes` if present (NOTE: the committed ext4 fixture has `mtime_nano=0` per Assumption A3, so this branch has no automated Wave-0 target — sub-second/nano MACB folding is verified manually per the 02-VALIDATION.md Manual-Only row; the code path is still implemented). Route EVERY value through `iso_utc` so naive datetimes are structurally impossible (it raises on naive). Do this only for entries that have meta (`meta_addr`/fields present); meta-less entries keep null MACB.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_walk.py::test_macb_utc_and_fat_flagged tests/test_walk.py::test_no_naive_datetimes -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_walk.py::test_macb_utc_and_fat_flagged -x` exits 0: ext4/NTFS MACB columns end with `+00:00`; the FAT fixture's rows carry `attributes["time_precision"]=="local-time-inferred"` and an `assumed_timezone`; a zero timestamp reads back as None.
    - `pytest tests/test_walk.py::test_no_naive_datetimes -x` exits 0: every non-null `*_utc` column re-parses via `datetime.fromisoformat` to a datetime whose `tzinfo` is not None and ends `+00:00`.
    - Source assertion: `grep -c 'import pytsk3' src/pyautopsy/core/walk.py` is 0 (D-14 — walk.py is not a seam).
    - Source assertion: `grep -c 'timestamp_source' src/pyautopsy/core/walk.py` is >= 1.
    - Behavior (D-16 exact value): `pytest tests/test_walk.py::test_macb_utc_and_fat_flagged -x` (or a sibling node) asserts the ext4 fixture rows have `timestamp_source == "ext4:inode"`, the NTFS fixture rows `"ntfs:$STANDARD_INFORMATION"`, and the FAT fixture rows `"fat:dir-entry"` — exact strings, not mere presence.
  </acceptance_criteria>
  <done>MACB times persist as tz-aware UTC ISO-8601 with explicit offset; FAT flagged local-time-inferred; zero→None; timestamp_source recorded; no naive datetimes possible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ownership (UID/GID) and permission/mode bits per file</name>
  <files>src/pyautopsy/core/walk.py</files>
  <read_first>
    - src/pyautopsy/core/walk.py (the per-entry FileRow build + the Task 1 MACB helper)
    - src/pyautopsy/evidence/filesystem.py (FileEntry.uid/gid/mode plain-int fields from Plan 01 Task 2)
    - 02-RESEARCH.md META-03 row (line 42: `meta.uid`/`meta.gid`/`meta.mode` verified on all three filesystems) + Code Examples (lines 427)
  </read_first>
  <behavior>
    - A FileEntry with uid=0/gid=0/mode set → FileRow.uid==0, FileRow.gid==0, FileRow.mode==<mode int>, persisted and read back equal.
    - A meta-less entry → uid/gid/mode are None (not 0).
  </behavior>
  <action>
    In `core/walk.py`, populate `FileRow.uid`/`gid`/`mode` directly from the plain-int `FileEntry.uid`/`gid`/`mode` fields (already extracted behind the seam in Plan 01). Preserve `None` for meta-less entries (do NOT coerce to 0). These are integers persisted in the typed columns added in Plan 01; no schema change. Keep the mode as the raw integer mode bits (the report layer in Phase 3 formats it) — do not pre-format here.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_walk.py::test_ownership_and_mode -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_walk.py::test_ownership_and_mode -x` exits 0: the known fixture file's uid/gid/mode match the values written by the fixture builder (Plan 00 ground-truth constants).
    - Behavior: a meta-less entry's uid/gid/mode read back as None.
  </acceptance_criteria>
  <done>UID/GID and mode bits recorded per file from the seam's plain-int fields; meta-less entries keep null ownership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seam epochs → timeline | Attacker-controlled timestamps must not poison the UTC timeline with naive/local values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-02-NAIVE | Information Disclosure / Integrity | MACB normalization in walk.py | mitigate | Every MACB value routed through `iso_utc`, which raises on naive datetimes — making a naive/local-time value structurally impossible to persist (D-10/P4). Verified by `test_no_naive_datetimes`. |
| T-2-02-FAT | Integrity (overclaiming) | FAT local-time rows | mitigate | FAT epochs rebased via `--timezone` and flagged `local-time-inferred` + `assumed_timezone` so the report never overclaims FAT precision (D-16/Pitfall 2). Verified by `test_macb_utc_and_fat_flagged`. |
| T-2-02-TZ | Input Validation | `--timezone` option | mitigate | Validated via `zoneinfo.ZoneInfo(tz)` at the CLI (Plan 01 Task 3) — raises on a bad zone before the walk (Security V5). |
</threat_model>

<verification>
- `python3 -m pytest tests/test_walk.py -x` (META-02 + META-03 nodes green).
- `python3 -m pytest` full suite green.
- `grep -c 'import pytsk3' src/pyautopsy/core/walk.py` is 0 (D-14 preserved).
</verification>

<success_criteria>
- MACB tz-aware UTC ISO-8601 with explicit offset; FAT flagged; zero→None; timestamp_source set (META-02).
- No naive datetimes can be persisted (success-criterion 2 invariant).
- UID/GID and mode bits recorded per file (META-03).
</success_criteria>

<output>
Create `.planning/phases/02-filesystem-walk-metadata/02-02-SUMMARY.md` when done
</output>
