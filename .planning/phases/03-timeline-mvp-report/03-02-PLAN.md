---
phase: 03-timeline-mvp-report
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/pyautopsy/report/__init__.py
  - src/pyautopsy/report/assemble.py
  - src/pyautopsy/report/jsonreport.py
  - src/pyautopsy/report/htmlreport.py
  - src/pyautopsy/report/templates/report.html.j2
autonomous: true
requirements: [REPORT-03, REPORT-04, CLI-02]
must_haves:
  truths:
    - "assemble_report_body returns ONE deterministic dict (no datetime.now reachable) covering the 8 canonical sections, including the full timeline AND timeline_total (M)"
    - "JSON report carries the FULL D-26-ordered timeline; HTML carries a bounded slice (body['timeline'][:cap]) with an honest 'Showing N of M' truncation note derived from timeline_total"
    - "Evidence-controlled strings render through Jinja2 autoescape; no | safe on evidence values"
    - "report.html carries ZERO run metadata (W-1); run metadata lives only in the run_metadata.json sidecar written by 03-03"
    - "Findings = inventory stats + integrity PASS/FAIL + D-20 limitations; FAT local-time-inferred / assumed_timezone provenance survives; tsk_version + pyautopsy_version recorded"
    - "report.html and report.json are written only inside case_dir/reports/ (path-confined)"
  artifacts:
    - path: "src/pyautopsy/report/assemble.py"
      provides: "assemble_report_body deterministic body dict (incl. timeline_total) + run_metadata builder"
      exports: ["assemble_report_body"]
      min_lines: 60
    - path: "src/pyautopsy/report/jsonreport.py"
      provides: "write_json (sort_keys, ensure_ascii=False, confined)"
      exports: ["write_json"]
    - path: "src/pyautopsy/report/htmlreport.py"
      provides: "render_html (Jinja2 autoescape env, in-process bounded slice, no run metadata, confined)"
      exports: ["render_html"]
    - path: "src/pyautopsy/report/templates/report.html.j2"
      provides: "self-contained offline report template (8 sections, inline CSS, no run metadata)"
      contains: "Showing"
  key_links:
    - from: "src/pyautopsy/report/htmlreport.py"
      to: "report/templates/report.html.j2"
      via: "Jinja2 Environment(autoescape=select_autoescape, trim_blocks, lstrip_blocks, keep_trailing_newline)"
      pattern: "select_autoescape"
    - from: "src/pyautopsy/report/htmlreport.py"
      to: "body['timeline'][:cap]"
      via: "in-process slice of the body's full timeline list; honest 'Showing {cap} of {timeline_total}' from body['timeline_total'] (NO store handle)"
      pattern: "\\[:cap\\]"
    - from: "src/pyautopsy/report/assemble.py"
      to: "CaseStore read API"
      via: "get_case/get_evidence_source/get_files/get_volume_limitations/get_timeline_events"
      pattern: "get_timeline_events"
---

<objective>
Build the reporter (REPORT-03 / REPORT-04 / determinism foundation for CLI-02): one deterministic `assemble_report_body()` consumed by both a JSON writer (full unabridged timeline) and a Jinja2 HTML renderer (bounded in-process slice + honest truncation), per D-22 (HTML via Jinja2; PDF deferred but the template is authored to allow the later WeasyPrint path). Findings per D-28, run-metadata segregated to a sidecar per D-25/W-1 (report.html carries NO run metadata), output confined to `case_dir/reports/`. This slice makes a report visible from already-persisted rows, fulfilling the human + structured deliverables before the single-command wrapper.

Purpose: One canonical body dict is the determinism single-source-of-truth (Pattern 3); autoescape + path confinement are the security controls; D-27/D-28 honesty is load-bearing forensic correctness.
Output: `report/` package (`assemble.py`, `jsonreport.py`, `htmlreport.py`, `templates/report.html.j2`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-timeline-mvp-report/03-RESEARCH.md
@.planning/phases/03-timeline-mvp-report/03-PATTERNS.md
@.planning/phases/03-timeline-mvp-report/03-UI-SPEC.md
@.planning/phases/03-timeline-mvp-report/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: assemble_report_body + write_json (deterministic body, full JSON timeline, timeline_total)</name>
  <files>src/pyautopsy/report/__init__.py, src/pyautopsy/report/assemble.py, src/pyautopsy/report/jsonreport.py</files>
  <read_first>
    - src/pyautopsy/audit/log.py (json.dumps(sort_keys=True, ensure_ascii=False) primitive log.py:104; _is_within confinement log.py:122-124; AuditLog.__init__ confined-write log.py:52-57)
    - src/pyautopsy/case/store.py (get_case store.py:226, get_evidence_source store.py:292 — note EvidenceSource.case_id links to Case; get_files store.py:408, get_volume_limitations store.py:484, get_timeline_events from 03-00; _SUBDIRS store.py:37 + create subdir loop store.py:88-92)
    - src/pyautopsy/case/models.py (Case pyautopsy_version models.py:46; EvidenceSource case_id/md5/sha256/tsk_version models.py:70-77; FileRow.attributes provenance flags models.py:120)
    - src/pyautopsy/core/walk.py (deleted detection: `allocated is False` walk.py:555; provenance flag origins time_precision/assumed_timezone walk.py:202-203, file_type_provenance walk.py:366)
    - .planning/phases/03-timeline-mvp-report/03-UI-SPEC.md (Report Section Hierarchy lines 123-137; copywriting contract lines 162-176; determinism constraints lines 196-203)
    - .planning/phases/03-timeline-mvp-report/03-RESEARCH.md (Pattern 3 lines 254-262; determinism primitive lines 313-320; Open Questions RESOLVED — sidecar-only run metadata)
    - .planning/phases/03-timeline-mvp-report/03-PATTERNS.md (assemble.py + jsonreport.py assignments lines 101-122)
  </read_first>
  <behavior>
    - assemble_report_body(store, esid) returns a plain dict with the 8 canonical sections (UI-SPEC:123-137) in fixed key structure, containing only analytical content; no datetime.now/utc_now reachable.
    - Findings (D-28) = file count, directory count, deleted-entry count (allocated is False), per-volume breakdown, file-type distribution, integrity PASS/FAIL (acquisition compare + re-verify), and volume limitations; FAT provenance flags from FileRow.attributes are surfaced, not dropped.
    - The body's timeline is the FULL list from get_timeline_events (no limit) in D-26 order, AND the body carries timeline_total (M) = len(timeline) so HTML can render an honest "Showing N of M" without a store handle (W-2 lock).
    - write_json(body, case_dir) serializes with json.dumps(sort_keys=True, ensure_ascii=False), one locked trailing-newline convention, writes only to case_dir/reports/report.json confined via _is_within.
    - A separate build_run_metadata(...) produces the volatile dict (generation timestamp via the sanctioned UTC helper, host, durations, run-id). It is NEVER merged into the body and is consumed by 03-03 to write the run_metadata.json sidecar — it is NOT passed to render_html (report.html carries no run metadata, W-1).
  </behavior>
  <action>
Create src/pyautopsy/report/__init__.py exporting `assemble_report_body`, `write_json`, `render_html`. Add `"reports"` to `_SUBDIRS` in src/pyautopsy/case/store.py — WAIT: store.py is owned by 03-00; instead create the `reports/` subdir in the reporter using the `_is_within` + mkdir(parents=True, exist_ok=True) idiom (do not edit store.py here). [Resolved: create reports/ in the reporter to keep file ownership disjoint from Wave 0.]

src/pyautopsy/report/assemble.py: define `assemble_report_body(store: CaseStore, evidence_source_id: int) -> dict[str, Any]` — read `ev = store.get_evidence_source(esid)`, `case = store.get_case(ev.case_id)`, `files = store.get_files(esid)`, `limitations = store.get_volume_limitations(esid)`, `timeline = store.get_timeline_events(esid)` (FULL, no limit). Build a dict keyed by the 8 UI-SPEC sections: `header` (title `PyAutopsy Forensic Report — Case {case_id}`, case_id, examiner, evidence_id, acquisition source), `integrity` (PASS/FAIL booleans + the literal UI-SPEC copy strings for acquisition-compare and end-of-run re-verify), `methodology` (pinned `tsk_version` + `pyautopsy_version` verbatim), `findings` (4a inventory counts: total/dir/deleted via `allocated is False`; 4b per-volume breakdown; 4c file-type distribution ranked), `evidence_hashes` (md5/sha1/sha256), `timeline` (the full ordered list), `limitations` (D-20 volume limitations + the standing MVP-limitations copy string from UI-SPEC:171), and section-level provenance surfacing of `time_precision`/`assumed_timezone`/`file_type_provenance` from FileRow.attributes. ALSO add `timeline_total` = `len(timeline)` (M) to the body so render_html can disclose "Showing N of M" without re-reading the store (W-2 lock). NO `datetime.now()`/`utc_now()`/`iso_utc()` reachable from this function. Aggregate counts by sorting keys deterministically (sort file-type distribution by `(-count, type)`); never rely on dict/set iteration order for output.

Add a separate `build_run_metadata(*, generation_ts: str, host: str, durations: dict, run_id: str | None = None) -> dict` that the caller fills with volatile values (this is the ONLY place wall-clock lives — D-25). It is never merged into the body and never passed to render_html; 03-03 writes it to the `reports/run_metadata.json` sidecar (W-1: report.html carries no run metadata).

src/pyautopsy/report/jsonreport.py: define `write_json(body: dict, case_dir: Path) -> Path` — compute `reports_dir = case_dir / "reports"`, validate with the `_is_within(reports_dir, case_dir)` realpath check (copy the helper or import the pattern from audit/log.py:122-124), `mkdir(parents=True, exist_ok=True)`, write `report.json` as `json.dumps(body, sort_keys=True, ensure_ascii=False)` with a locked trailing-newline convention (pick "no trailing newline" and assert it in the CLI-02 test). Return the path. Build the path only from case_dir + the fixed name, never from evidence content (Pitfall 4). report.json carries the body only — no run metadata.
  </action>
  <verify>
    <automated>python -m pytest tests/test_report.py::test_json_report tests/test_report.py::test_findings_d28 tests/test_report.py::test_fat_provenance tests/test_report.py::test_versions_recorded -x -q && ruff check src/pyautopsy/report && mypy src/pyautopsy/report</automated>
  </verify>
  <acceptance_criteria>
    - `src/pyautopsy/report/assemble.py` contains `def assemble_report_body(` and `def build_run_metadata(` and emits a `timeline_total` key
    - `grep -n "datetime.now\|utc_now\|iso_utc" src/pyautopsy/report/assemble.py` returns nothing
    - `src/pyautopsy/report/jsonreport.py` contains `json.dumps(` with `sort_keys=True` and `ensure_ascii=False`
    - `python -m pytest tests/test_report.py::test_json_report tests/test_report.py::test_findings_d28 tests/test_report.py::test_fat_provenance tests/test_report.py::test_versions_recorded -x -q` exits 0
    - report.json is written under `<case_dir>/reports/` only (path-confinement test passes)
  </acceptance_criteria>
  <done>assemble_report_body emits a deterministic 8-section body with D-28 findings + surfaced provenance + pinned versions + timeline_total; write_json emits the full timeline as sorted-key UTF-8 JSON confined to reports/; no wall-clock in the body.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: render_html (Jinja2 autoescape, in-process bounded timeline, no run metadata) + report.html.j2 template</name>
  <files>src/pyautopsy/report/htmlreport.py, src/pyautopsy/report/templates/report.html.j2</files>
  <read_first>
    - src/pyautopsy/audit/log.py (_is_within log.py:122-124 — reuse for HTML path confinement)
    - src/pyautopsy/report/assemble.py + jsonreport.py (from Task 1 — body shape incl. timeline_total, and the reports/ confinement idiom)
    - .planning/phases/03-timeline-mvp-report/03-UI-SPEC.md (FULL — section order 123-137, status encoding 107-118, copy contract 162-176, bounded-timeline disclosure 147-154, color/typography/spacing, print rules 185-192, determinism 196-203)
    - .planning/phases/03-timeline-mvp-report/03-RESEARCH.md (Jinja2 env setup lines 322-341; packaging note; Pitfalls 3/5; Open Questions RESOLVED — report.html carries NO run metadata)
    - .planning/phases/03-timeline-mvp-report/03-PATTERNS.md (htmlreport.py + template assignments lines 126-148)
  </read_first>
  <behavior>
    - render_html builds a Jinja2 Environment with autoescape=select_autoescape(["html","j2"]), trim_blocks=True, lstrip_blocks=True, keep_trailing_newline=True, loading templates via importlib.resources.files("pyautopsy.report")/"templates".
    - HTML renders the 8 sections in canonical order; an evidence path/name containing `<script>` renders escaped (no live markup).
    - The HTML timeline is the in-process slice `body["timeline"][:cap]` (default cap 2000, configurable); when total M (body["timeline_total"]) > cap an unmissable "Showing N of M timeline events..." disclosure renders; when N == M no disclosure shows. render_html takes NO store handle (W-2 lock).
    - report.html carries ZERO run metadata (W-1 lock) — no footer block, no embedded generation timestamp/host/durations/run-id; report.html is therefore byte-deterministic across runs. Run metadata lives only in the run_metadata.json sidecar (written by 03-03).
    - report.html written only under case_dir/reports/ (confined); template references zero http(s)/CDN/web-font/external resources.
  </behavior>
  <action>
src/pyautopsy/report/htmlreport.py: define `render_html(body: dict, case_dir: Path, *, cap: int = 2000) -> Path` (NO `run_metadata` parameter, NO store handle — W-1/W-2) and a module-level `_build_env()` using the verbatim setup from 03-RESEARCH.md:322-341 (`importlib.resources.files`, `FileSystemLoader`, `select_autoescape(["html","j2"])`, `trim_blocks`, `lstrip_blocks`, `keep_trailing_newline`). Compute the bounded slice IN-PROCESS from the body: `shown = body["timeline"][:cap]`; read the true total `M = body["timeline_total"]`. Render `report.html.j2` with `body=body`, `timeline=shown`, `timeline_total=M`, `cap=cap` (do NOT pass run_metadata — report.html has none). Never re-read the store and never materialize-then-slice a fresh full list (the body already holds the full list; slicing it in-process is O(cap) and bounded — Pitfall 5). Confine the output path with `_is_within(reports_dir, case_dir)` exactly like jsonreport, write `report.html`.

src/pyautopsy/report/templates/report.html.j2: author the self-contained template per 03-UI-SPEC.md (authoritative). One inline `<style>` block, system-font stacks only, no http(s)/@import/web-font/external image. Eight sections in the canonical order (UI-SPEC:123-137). Status encoding per UI-SPEC:107-118 — every status carries a text label + glyph (PASS/✓, FAIL/✗, deleted/unallocated tag column, local-time-inferred/⚠, assumed tz/⚠, limitation/⚠), never color-only. Integrity FAIL prominent near the top with the exact UI-SPEC:167 wording. Timeline table columns left→right in D-26 order (timestamp +00:00 → volume → path → event type → meta_addr → provenance flags), iterating the already-sorted `timeline` slice (never a dict/set). Truncation disclosure with real `{timeline|length}`/`{timeline_total}` numbers per UI-SPEC:170, shown only when `timeline_total > timeline|length`. Mandatory Limitations section incl. the standing MVP copy (UI-SPEC:171). The template MUST contain NO run-metadata footer/section and NO reference to generation timestamp/host/durations/run-id (W-1 — run metadata lives only in run_metadata.json). Print rules: `@page { size: A4; margin: 16mm; }`, no vh/vw, break-inside/avoid on small cards, overflow-wrap:anywhere on hashes/paths. NEVER use `| safe`/`Markup()` on any evidence-derived value. Verify `.j2` ships in the wheel (03-RESEARCH.md packaging note); add a `force-include`/`artifacts` entry to pyproject.toml ONLY if a built wheel omits it.
  </action>
  <verify>
    <automated>python -m pytest tests/test_report.py::test_html_autoescape tests/test_report.py::test_html_truncation_note -x -q && ruff check src/pyautopsy/report && mypy src/pyautopsy/report</automated>
  </verify>
  <acceptance_criteria>
    - `src/pyautopsy/report/htmlreport.py` contains `select_autoescape(` and slices `body["timeline"][:cap]` (in-process bounded read, no store handle)
    - `grep -n "run_metadata" src/pyautopsy/report/htmlreport.py src/pyautopsy/report/templates/report.html.j2` returns nothing (report.html carries no run metadata — W-1)
    - `grep -n "| safe\|Markup(" src/pyautopsy/report/templates/report.html.j2` returns nothing
    - `grep -n "http://\|https://\|@import\|//cdn" src/pyautopsy/report/templates/report.html.j2` returns nothing (offline/self-contained)
    - `grep -n "vh\|vw\|vmin" src/pyautopsy/report/templates/report.html.j2` returns no viewport units in CSS
    - `python -m pytest tests/test_report.py::test_html_autoescape tests/test_report.py::test_html_truncation_note -x -q` exits 0 (a `<script>` filename renders as `&lt;script&gt;`; "Showing N of M" appears when M > cap)
  </acceptance_criteria>
  <done>render_html produces an autoescaped, offline, print-correct report.html with a bounded in-process D-26-ordered timeline slice + honest truncation note, NO run metadata, confined to reports/; template carries no | safe, no external/CDN refs, no viewport units, no run-metadata footer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence-controlled strings → report.html | Filenames/paths/file-type/limitation strings are adversarial and rendered into HTML — the primary injection surface. |
| caller-supplied case_dir → report file paths | Output paths must stay inside case_dir. |
| huge real timeline → HTML rendering | Multi-million-event timelines can exhaust memory if fully materialized for HTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering / Elevation | report.html.j2 evidence values | mitigate | Jinja2 autoescape ON (select_autoescape html/j2); no `| safe`/`Markup()` on evidence values; tested with a `<script>`-bearing filename (test_html_autoescape). |
| T-03-07 | Tampering | report file output path | mitigate | Paths built only from case_dir + fixed names (report.json/report.html); reuse audit/log.py `_is_within` realpath confinement; create reports/ like logs/. |
| T-03-08 | Denial of Service | HTML timeline rendering | mitigate | D-27 bounded view via in-process `body["timeline"][:cap]`; render_html slices a bounded prefix, renders only `cap` rows (Pitfall 5). |
| T-03-09 | Information Disclosure (misleading) | truncation / findings honesty | mitigate | Real "Showing N of M" disclosure from body["timeline_total"] (only when truncated); mandatory Limitations section with standing MVP disclaimer; FAT provenance flags surfaced; no human-intent phrasing (UI-SPEC tone rules). |
| T-03-10 | Repudiation | non-deterministic markup | mitigate | No datetime.now/run-id/host in body markup; report.html carries ZERO run metadata (W-1); deterministic Jinja2 whitespace; iterate already-sorted timeline list (foundation for the CLI-02 whole-file byte test on report.html in 03-03). |
</threat_model>

<verification>
- `python -m pytest tests/test_report.py -q` green (json, autoescape, truncation, d28 findings, fat provenance, versions recorded).
- Template carries no `| safe`, no http(s)/CDN/web-font/external refs, no viewport units, no run-metadata footer.
- Report files written only under case_dir/reports/.
- `ruff check src/pyautopsy/report && mypy src/pyautopsy/report` clean.
</verification>

<success_criteria>
- One deterministic body dict (incl. timeline_total) feeds both JSON (full timeline) and HTML (in-process bounded slice + honest truncation).
- Autoescape neutralizes evidence-controlled HTML; path confinement holds.
- report.html carries no run metadata (byte-deterministic across runs); D-28 findings + surfaced provenance + pinned versions present; mandatory limitations section.
</success_criteria>

<output>
Create `.planning/phases/03-timeline-mvp-report/03-02-SUMMARY.md` when done.
</output>
