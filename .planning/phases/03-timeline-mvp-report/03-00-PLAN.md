---
phase: 03-timeline-mvp-report
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - src/pyautopsy/case/schema.sql
  - src/pyautopsy/case/models.py
  - src/pyautopsy/case/__init__.py
  - src/pyautopsy/case/store.py
  - tests/test_timeline.py
  - tests/test_report.py
  - tests/test_analyze.py
  - tests/test_reproducibility.py
autonomous: true
requirements: [TIME-01, REPORT-03, REPORT-04, CLI-01, CLI-02]
must_haves:
  truths:
    - "timeline_events table exists in a freshly created case.db with the D-26 ordering index"
    - "CaseStore can persist and read back TimelineEvent rows in D-26 total order"
    - "Failing RED tests exist for every Phase 3 requirement (test_timeline, test_report, test_analyze, extended test_reproducibility)"
    - "test_total_order asserts the FULL value-level ordered sequence on a fixture with events tying on ts+volume+offset+path (differing only on event_type + meta_addr), so dropping any D-26 tiebreak fails the test"
    - "Jinja2 is a declared pure-Python dependency"
  artifacts:
    - path: "src/pyautopsy/case/schema.sql"
      provides: "timeline_events DDL + idx_timeline_events_order + idx_timeline_events_evidence_source_id"
      contains: "CREATE TABLE IF NOT EXISTS timeline_events"
    - path: "src/pyautopsy/case/models.py"
      provides: "TimelineEvent frozen-slots dataclass"
      contains: "class TimelineEvent"
    - path: "src/pyautopsy/case/store.py"
      provides: "insert_timeline_events + get_timeline_events"
      contains: "def get_timeline_events"
    - path: "tests/test_timeline.py"
      provides: "RED scaffold for TIME-01 (incl. value-level test_total_order)"
    - path: "tests/test_report.py"
      provides: "RED scaffold for REPORT-03/REPORT-04"
    - path: "tests/test_analyze.py"
      provides: "RED scaffold for CLI-01"
    - path: "pyproject.toml"
      provides: "jinja2 dependency declaration"
      contains: "jinja2"
  key_links:
    - from: "src/pyautopsy/case/store.py get_timeline_events"
      to: "schema.sql idx_timeline_events_order"
      via: "ORDER BY ts_utc, volume_id, volume_offset, path, event_type, meta_addr"
      pattern: "ORDER BY ts_utc"
    - from: "src/pyautopsy/case/store.py insert_timeline_events"
      to: "timeline_events table"
      via: "executemany over _TIMELINE_INSERT_SQL inside transaction"
      pattern: "_TIMELINE_INSERT_SQL"
---

<objective>
Establish the Phase 3 contract surface and RED test scaffold (Wave 0). Add the `timeline_events` table (the shared forensic-event model, D-23), the `TimelineEvent` model (D-24 field shape), and the `CaseStore` insert/D-26-ordered-read path — the interface every later wave consumes. Declare Jinja2. Author failing tests for all five requirements so subsequent waves drive them green.

Purpose: Interface-first — the schema, model, and store read/write API are the contracts Waves 1–3 build against; the RED tests are the executable acceptance bar (per 03-VALIDATION.md).
Output: `timeline_events` schema + index, `TimelineEvent` dataclass, `insert_timeline_events`/`get_timeline_events`, declared `jinja2` dep, and failing `tests/test_timeline.py` / `tests/test_report.py` / `tests/test_analyze.py` + extended `tests/test_reproducibility.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-timeline-mvp-report/03-CONTEXT.md
@.planning/phases/03-timeline-mvp-report/03-RESEARCH.md
@.planning/phases/03-timeline-mvp-report/03-PATTERNS.md
@.planning/phases/03-timeline-mvp-report/03-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: timeline_events schema + TimelineEvent model + store CRUD with D-26 ordered read</name>
  <files>src/pyautopsy/case/schema.sql, src/pyautopsy/case/models.py, src/pyautopsy/case/__init__.py, src/pyautopsy/case/store.py</files>
  <read_first>
    - src/pyautopsy/case/schema.sql (the `files` block schema.sql:63-95 and `volume_limitations` block schema.sql:101-112 — copy table/index conventions exactly)
    - src/pyautopsy/case/models.py (FileRow models.py:83-148 — frozen-slots dataclass pattern, attributes+id as last two fields, Args docstring; __all__ models.py:17)
    - src/pyautopsy/case/store.py (insert_files store.py:343-361, get_files store.py:408-421, _FILES_COLUMNS/_FILES_INSERT_SQL/_file_row_params store.py:506-569, _load_attributes store.py:572, _commit_unless_in_transaction store.py:184)
    - src/pyautopsy/case/__init__.py (re-export list)
    - .planning/phases/03-timeline-mvp-report/03-RESEARCH.md (DDL + ordered read lines 346-374; explode field mapping lines 217-235)
    - .planning/phases/03-timeline-mvp-report/03-PATTERNS.md (schema/model/store assignments lines 31-87)
  </read_first>
  <behavior>
    - A freshly created case.db (CaseStore.create) contains a timeline_events table and both indexes.
    - insert_timeline_events([events]) persists rows; get_timeline_events(esid) returns them reconstructed as TimelineEvent with attributes parsed.
    - get_timeline_events applies the D-26 total order regardless of insertion order: ts_utc → volume_id → volume_offset → path → event_type → meta_addr.
    - get_timeline_events accepts an optional limit param returning only the first N in D-26 order (for the D-27 bounded HTML slice).
    - insert_timeline_events composes inside an outer `with store.transaction():` (no premature commit).
  </behavior>
  <action>
Add a `timeline_events` table to src/pyautopsy/case/schema.sql mirroring the `files` block conventions: `id INTEGER PRIMARY KEY AUTOINCREMENT`, `evidence_source_id INTEGER NOT NULL REFERENCES evidence_sources (id)`, `file_id INTEGER REFERENCES files (id)` (nullable for Phase 5 log events), `ts_utc TEXT NOT NULL`, `source TEXT NOT NULL`, `event_type TEXT NOT NULL`, `volume_id INTEGER NOT NULL`, `volume_offset INTEGER NOT NULL`, `path TEXT NOT NULL`, `meta_addr INTEGER`, `actor TEXT`, `action TEXT`, `outcome TEXT`, `attributes TEXT NOT NULL DEFAULT '{}'`. Add a header comment block in the `files`-comment style. Add `idx_timeline_events_order` ON `(ts_utc, volume_id, volume_offset, path, event_type, meta_addr)` and `idx_timeline_events_evidence_source_id` — use the verbatim DDL/index from 03-RESEARCH.md:346-368.

Add a `TimelineEvent` `@dataclass(frozen=True, slots=True)` to src/pyautopsy/case/models.py with the field shape from 03-PATTERNS.md:48-65: required `evidence_source_id: int`, `ts_utc: str`, `source: str`, `event_type: str`, `volume_id: int`, `volume_offset: int`, `path: str`; then `meta_addr: int | None = None`, `actor: str | None = None`, `action: str | None = None`, `outcome: str | None = None`, `file_id: int | None = None`, `attributes: dict[str, Any] = field(default_factory=dict)`, `id: int | None = None` as the last two. Full Args docstring per field; ts_utc is a UTC ISO-8601 string copied verbatim from the walk (never re-derived). Add `"TimelineEvent"` to `__all__` and re-export from src/pyautopsy/case/__init__.py.

In src/pyautopsy/case/store.py add module-level `_TIMELINE_COLUMNS` tuple, derive `_TIMELINE_INSERT_SQL` via `", ".join(...)` exactly like `_FILES_INSERT_SQL` (store.py:533), and a `_timeline_event_params(event)` that serialises `attributes` via `json.dumps(event.attributes, sort_keys=True)` (cf. store.py:568). Add `insert_timeline_events(self, rows: Iterable[TimelineEvent]) -> int` copying the `insert_files` structure (store.py:343-361) including `_commit_unless_in_transaction()`. Add `get_timeline_events(self, evidence_source_id: int, *, limit: int | None = None) -> list[TimelineEvent]` that runs `SELECT * FROM timeline_events WHERE evidence_source_id = ? ORDER BY ts_utc, volume_id, volume_offset, path, event_type, meta_addr` (the SINGLE place read ordering is defined — D-26), appending `LIMIT ?` only when `limit` is not None, reconstructing rows via `_load_attributes(row["attributes"])`. No raw SQL outside store.py.
  </action>
  <verify>
    <automated>python -m pytest tests/test_case_store.py -q && ruff check src/pyautopsy/case && mypy src/pyautopsy/case</automated>
  </verify>
  <acceptance_criteria>
    - `src/pyautopsy/case/schema.sql` contains `CREATE TABLE IF NOT EXISTS timeline_events` and `CREATE INDEX IF NOT EXISTS idx_timeline_events_order`
    - `src/pyautopsy/case/models.py` contains `class TimelineEvent` and `"TimelineEvent"` appears in `__all__`
    - `src/pyautopsy/case/store.py` contains `def get_timeline_events(` and `def insert_timeline_events(` and `ORDER BY ts_utc, volume_id, volume_offset, path, event_type, meta_addr`
    - `grep -n "import pytsk3\|import pyewf" src/pyautopsy/case/store.py src/pyautopsy/case/models.py` returns nothing (D-14 seam untouched)
    - `mypy src/pyautopsy/case` exits 0
  </acceptance_criteria>
  <done>timeline_events table, TimelineEvent model, and ordered insert/read CRUD exist; store/case mypy + ruff clean; the D-26 ORDER BY lives only in get_timeline_events.</done>
</task>

<task type="auto">
  <name>Task 2: Declare jinja2 + author failing RED tests for all Phase 3 requirements</name>
  <files>pyproject.toml, tests/test_timeline.py, tests/test_report.py, tests/test_analyze.py, tests/test_reproducibility.py</files>
  <read_first>
    - pyproject.toml (dependencies list pyproject.toml:26-31; [tool.hatch.build.targets.wheel] pyproject.toml:48-49)
    - tests/test_walk.py (module docstring + RED-scaffold + fixture-helper style test_walk.py:1-88; exact-count assertions test_walk.py:28-30)
    - tests/test_reproducibility.py (_RUN_METADATA_COLUMNS:27, _ingest helper:30-45, two-runs structure:75-93, segregation test:96-120)
    - tests/conftest.py (tiny_ext4_image:53, tiny_fat32_image:75 fixtures)
    - .planning/phases/03-timeline-mvp-report/03-RESEARCH.md (Phase Requirements→Test Map; CLI-02 byte-identical test design — sidecar-only / whole-file byte-equality on report.json AND report.html; Wave 0 Gaps)
    - .planning/phases/03-timeline-mvp-report/03-PATTERNS.md (test analogs lines 197-224)
  </read_first>
  <behavior>
    - All new test node IDs from the RESEARCH Test Map exist and currently FAIL (RED) for a real reason (missing module/function), not collection error.
    - test_total_order is VALUE-LEVEL: it asserts the exact ordered list of identifiers produced from a hand-built, deliberately-tied fixture, and fails if ANY D-26 tiebreak key is dropped (W-3).
    - test_reproducibility.py gains a `_analyze` CliRunner helper and a `test_two_analyze_runs_byte_identical_report` asserting whole-file byte-equality on BOTH report.json and report.html (run_metadata.json excluded) plus a segregation negative-assert, all skipped/xfail-free and failing only because `analyze`/report don't exist yet.
  </behavior>
  <action>
Add `"jinja2>=3.1,<4"` to `[project.dependencies]` in pyproject.toml (03-RESEARCH.md:104-107). Confirm `report/templates/*.j2` will ship via the already-listed `packages = ["src/pyautopsy"]`; only add a `force-include`/`artifacts` entry if a built wheel later omits it (A5) — leave a TODO comment if unsure, do not over-configure now.

Create RED test scaffolds with `from __future__ import annotations` and a module docstring naming the plan/req each covers (test_walk.py:1-12 style). Use the EXACT node IDs from the RESEARCH Test Map:
- tests/test_timeline.py: `test_macb_explosion` (each populated `*_utc` → one event, `None` → no event, correct event_type), `test_total_order`, `test_ext4_timeline` (integration on tiny_ext4_image — known file MACB times appear as events, deleted entry handled). Synthesize in-test FileRow/TimelineEvent objects (no new fixture image) for the unit cases.
  - `test_total_order` (W-3 — VALUE-LEVEL, not structural): hand-build a list of TimelineEvents that includes at least TWO events that TIE on `ts_utc` AND on `volume_id`/`volume_offset`/`path`, differing ONLY on `event_type` and `meta_addr` (plus the normal cross-file/cross-time cases). Feed them through the production ordering path (insert via `insert_timeline_events` then read back via `get_timeline_events`, OR the canonical Python `sorted(key=...)` used by the builder/reporter — whichever is the production sort site) and assert the FULL output sequence equals a hand-computed exact ordered list of identifiers (e.g. a list of `(path, event_type, meta_addr)` tuples, or stable per-event ids), value-by-value in order. The expected list MUST be constructed so that dropping ANY single key from the ORDER BY (volume_id → volume_offset → path → event_type → meta_addr) flips at least one adjacent pair and fails the assertion — in particular the event_type tie and the meta_addr tie each gate a distinct pair. Do NOT assert merely "sorted by ts_utc" or a length/structural check (Phase 2 lesson: structural tests do NOT catch wrong values; honor the Nyquist value-level mandate).
- tests/test_report.py: `test_json_report`, `test_html_autoescape` (a synthesized FileRow/TimelineEvent whose path/name contains `<script>` renders escaped — assert `&lt;script&gt;` present, `<script>` absent), `test_html_truncation_note` (events > cap → real "Showing N of M" disclosure), `test_findings_d28`, `test_fat_provenance` (uses tiny_fat32_image; `local-time-inferred`/`assumed_timezone` survive), `test_versions_recorded` (tsk_version + pyautopsy_version in report).
- tests/test_analyze.py: `test_analyze_produces_reports` (CliRunner `analyze` exits 0, writes reports/report.html + reports/report.json + reports/run_metadata.json), `test_analyze_composes_pipeline` (COC rows, files, timeline_events, AND end-of-run re-verify all present).
- Extend tests/test_reproducibility.py: add a `_analyze(image, case)` CliRunner helper mirroring `_ingest` (test_reproducibility.py:30-45); add `test_two_analyze_runs_byte_identical_report` asserting BOTH `(case_a/"reports"/"report.json").read_bytes() == (case_b/"reports"/"report.json").read_bytes()` AND `(case_a/"reports"/"report.html").read_bytes() == (case_b/"reports"/"report.html").read_bytes()` on tiny_ext4_image (whole-file byte-equality on both — report.html carries no run metadata, W-1), EXCLUDING `reports/run_metadata.json` from the comparison, with a non-empty body sanity assert and a negative assert that the segregated `reports/run_metadata.json` generation timestamp IS present and DOES differ between runs (03-RESEARCH CLI-02 test design).

Reference timeline build / report / analyze entry points by their planned import paths (`pyautopsy.timeline.builder.build_timeline`, `pyautopsy.report...`, `pyautopsy.core.analyze.run_analyze`) so the tests fail on ImportError until later waves land them. Do NOT add watch-mode flags.
  </action>
  <verify>
    <automated>python -m pytest tests/test_timeline.py tests/test_report.py tests/test_analyze.py tests/test_reproducibility.py -q || true; python -m pytest tests/test_cli_smoke.py tests/test_ingest.py tests/test_walk.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "jinja2" pyproject.toml` matches in the `[project.dependencies]` list
    - `python -m pytest tests/test_timeline.py tests/test_report.py tests/test_analyze.py -q` reports the new node IDs as FAILED/errored (RED), not "no tests ran"
    - `grep -c "def test_" tests/test_timeline.py` ≥ 3 and the IDs `test_total_order`, `test_ext4_timeline` are present
    - `test_total_order` asserts a full exact ordered identifier sequence (value-level) on a tied fixture — NOT a length/structural-only check; the fixture ties on ts+volume+offset+path so event_type and meta_addr tiebreaks are each exercised (W-3)
    - `grep -n "test_two_analyze_runs_byte_identical_report\|read_bytes()" tests/test_reproducibility.py` matches, and the test reads BOTH report.json and report.html bytes
    - existing suites still green: `python -m pytest tests/test_cli_smoke.py tests/test_ingest.py tests/test_walk.py -q` exits 0
  </acceptance_criteria>
  <done>jinja2 declared; RED scaffolds for TIME-01/REPORT-03/REPORT-04/CLI-01 exist with the canonical node IDs and fail for the right reason; test_total_order is value-level on a tied fixture (W-3); CLI-02 whole-file byte-identical test (report.json AND report.html) added to test_reproducibility.py; existing suites unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence image → case.db rows | Filenames/paths/file-type strings are attacker-controlled and become timeline_events/files rows this wave models. |
| developer test code → CI | Tests must fail honestly (RED), not silently pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | timeline_events.attributes JSON | mitigate | Serialize attributes via `json.dumps(sort_keys=True)` exactly like store.py:568; no eval/format of evidence strings into SQL (parameterized inserts only). |
| T-03-02 | Repudiation | get_timeline_events ordering | mitigate | D-26 total order in the ORDER BY guarantees deterministic, reproducible reads (the CLI-02 foundation); value-level test_total_order gates every tiebreak key (W-3). |
| T-03-03 | Tampering | D-14 native seam | accept | This plan adds pure-Python store/model/test code only; acceptance verified by grep gate (no pytsk3/pyewf imports outside the seam). |
| T-03-SC | Tampering | jinja2 install | accept | Jinja2/MarkupSafe are flagship Pallets packages, already installed (3.1.6), pure-Python — 03-RESEARCH Package Legitimacy Audit approved, no hand-verify gate warranted. |
</threat_model>

<verification>
- `python -m pytest tests/test_case_store.py -q` green (schema/model/store CRUD).
- New RED tests fail for the right reason (missing downstream modules), existing suites stay green.
- test_total_order is value-level on a tied fixture (W-3).
- `ruff check src/pyautopsy/case && mypy src/pyautopsy/case` clean.
- No pytsk3/pyewf import added outside the evidence seam.
</verification>

<success_criteria>
- timeline_events table + both indexes present in a fresh case.db.
- TimelineEvent persists and reads back in D-26 total order (incl. optional limit).
- jinja2 declared in pyproject.toml.
- RED scaffolds for all five requirements exist with canonical node IDs; test_total_order is value-level on a tied fixture; CLI-02 test compares report.json AND report.html whole-file.
</success_criteria>

<output>
Create `.planning/phases/03-timeline-mvp-report/03-00-SUMMARY.md` when done.
</output>
