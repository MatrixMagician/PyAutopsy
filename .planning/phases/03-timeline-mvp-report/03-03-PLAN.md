---
phase: 03-timeline-mvp-report
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/pyautopsy/core/analyze.py
  - src/pyautopsy/core/__init__.py
  - src/pyautopsy/cli/main.py
autonomous: true
requirements: [CLI-01, CLI-02]
must_haves:
  truths:
    - "pyautopsy analyze <image> --case ... runs ingest → walk → build-timeline → render-report in one process and writes reports/report.html + reports/report.json"
    - "analyze composes the existing run_ingest/run_walk (Phase 1 end-of-run re-verify still runs); ingest and walk remain standalone commands"
    - "analyze fails loudly if case.db already exists (fresh-case semantics, A2)"
    - "Two analyze runs on the same fixture produce BYTE-IDENTICAL report.json (and report.html body); run metadata is segregated and differs"
    - "analyze writes analyze.start/analyze.end audit events and maps forensic failures to the integrity exit code"
  artifacts:
    - path: "src/pyautopsy/core/analyze.py"
      provides: "run_analyze orchestrator + AnalyzeResult + AnalyzeError"
      exports: ["run_analyze", "AnalyzeResult", "AnalyzeError"]
      min_lines: 60
    - path: "src/pyautopsy/cli/main.py"
      provides: "analyze Typer command (thin shell)"
      contains: "def analyze"
  key_links:
    - from: "src/pyautopsy/core/analyze.py run_analyze"
      to: "run_ingest, run_walk, build_timeline, render_report"
      via: "sequential composition in one process"
      pattern: "run_ingest"
    - from: "src/pyautopsy/cli/main.py analyze command"
      to: "run_analyze"
      via: "thin Typer shell mapping forensic errors to integrity exit code"
      pattern: "run_analyze"
    - from: "tests/test_reproducibility.py"
      to: "reports/report.json"
      via: "two-run byte-equality assertion"
      pattern: "read_bytes()"
---

<objective>
Close the end-to-end MVP vertical slice (CLI-01) and prove reproducibility (CLI-02): `pyautopsy analyze <image> --case ...` composes the existing `run_ingest` and `run_walk` orchestrators, then builds the timeline and renders the report set — in one process, with the Phase 1 end-of-run re-verify still running. Lock byte-identical report bodies across runs with run-metadata segregated. `ingest`/`walk` remain standalone (no regression).

Purpose: This is the headline deliverable — one command, image → timeline + HTML + JSON report — and the reproducibility guarantee that makes the report defensible. It proves the spine before Phase 4/5 producers are added.
Output: `core/analyze.py` (`run_analyze`/`AnalyzeResult`/`AnalyzeError`), the `analyze` CLI command, and the passing CLI-02 byte-identical test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-timeline-mvp-report/03-RESEARCH.md
@.planning/phases/03-timeline-mvp-report/03-PATTERNS.md
@.planning/phases/03-timeline-mvp-report/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run_analyze orchestrator (compose ingest+walk+timeline+report, fresh-case, audit, segregated run metadata)</name>
  <files>src/pyautopsy/core/analyze.py, src/pyautopsy/core/__init__.py</files>
  <read_first>
    - src/pyautopsy/core/ingest.py (run_ingest signature ingest.py:129; IngestResult ingest.py:61-88 "analytical, never wall-clock"; IngestError ingest.py:50-57; re-verify runs inside run_ingest ingest.py:246-247)
    - src/pyautopsy/core/walk.py (run_walk signature walk.py:424-451; WalkResult walk.py:237-256; WalkError walk.py:207-214; _EXPECTED_*_ERRORS walk.py:225-233; FAIL-before-propagate + finally store.close walk.py:579-602; analyze.start/end audit analogs walk.py:485-491,570-578)
    - src/pyautopsy/case/store.py (CaseStore.open store.py:110; context-manager use store.py:141-150; create store.py:72-92)
    - src/pyautopsy/core/__init__.py (export list)
    - src/pyautopsy/timeline/builder.py (build_timeline, from 03-01) and src/pyautopsy/report (assemble_report_body/write_json/render_html + build_run_metadata, from 03-02)
    - src/pyautopsy/util/timeutil.py (utc_now/iso_utc — used ONLY for run_metadata generation timestamp, never the body)
    - .planning/phases/03-timeline-mvp-report/03-RESEARCH.md (Composing analyze lines 404-418, fresh-case decision A2, audit note)
    - .planning/phases/03-timeline-mvp-report/03-PATTERNS.md (analyze.py assignment lines 152-171)
  </read_first>
  <behavior>
    - run_analyze(image, case_dir, *, examiner, evidence_id, acquisition_hash=None, timezone="UTC", max_hash_size=None) fails loudly (AnalyzeError) if case_dir/case.db already exists (fresh-case, A2) BEFORE doing work.
    - It calls run_ingest(...) then run_walk(...) (re-verify runs inside run_ingest), then opens the store, build_timeline(store, esid), assembles the body, writes report.json and report.html into case_dir/reports/.
    - Run metadata (generation timestamp via utc_now, host, durations) is assembled separately via build_run_metadata and written ONLY to the segregated region/file — never into the body.
    - On expected forensic failure it writes an analyze.error/analyze.end FAIL audit event before propagating; genuine bugs write analyze.crashed; store closed in finally.
    - Returns AnalyzeResult carrying analytical-only fields (case_id, evidence_source_id, files_inventoried, deleted_count, event_count, report paths) — no wall-clock.
  </behavior>
  <action>
Create src/pyautopsy/core/analyze.py with `from __future__ import annotations`. Define `AnalyzeError(Exception)` like WalkError (walk.py:207-214). Define `AnalyzeResult` `@dataclass(frozen=True, slots=True)` copying the IngestResult shape (ingest.py:61-88) with the "analytical (reproducible) content — never wall-clock metadata" docstring: `case_id`, `evidence_source_id`, `files_inventoried`, `deleted_count`, `event_count`, `report_json_path`, `report_html_path`.

Define `run_analyze(...)` with the D-21 signature above. FIRST: if `(Path(case_dir) / "case.db").exists()` raise `AnalyzeError` (fresh-case semantics, A2 — cleanest determinism). Then compose per 03-RESEARCH.md:404-417: `ingest_result = run_ingest(image, case_dir, examiner=examiner, evidence_id=evidence_id, acquisition_hash=acquisition_hash)` (re-verify already runs inside, ingest.py:246-247); `walk_result = run_walk(image, case_dir, timezone=timezone, max_hash_size=max_hash_size)`; then `with CaseStore.open(case_dir) as store:` call `n_events = build_timeline(store, ingest_result.evidence_source_id)`, `body = assemble_report_body(store, ingest_result.evidence_source_id)`, `write_json(body, case_dir)`, `render_html(body, run_metadata, case_dir, ...)`. Assemble `run_metadata = build_run_metadata(generation_ts=utc_now-ISO, host=..., durations=...)` SEPARATELY (the ONLY wall-clock site, D-25) and pass it to render_html / write a run_metadata sidecar — keep it out of the body. Audit `analyze.start` / `analyze.end` like run_walk (walk.py:485-491,570-578); on expected errors (reuse an `_EXPECTED_ANALYZE_ERRORS` tuple including IntegrityError/MountedSourceError/ImageOpenError/WalkError/IngestError) write a FAIL audit then re-raise as/within AnalyzeError; on unexpected exceptions write `analyze.crashed`; `finally: store.close()`. Return AnalyzeResult (analytical-only). Export run_analyze/AnalyzeResult/AnalyzeError from src/pyautopsy/core/__init__.py.
  </action>
  <verify>
    <automated>python -m pytest tests/test_analyze.py -x -q && ruff check src/pyautopsy/core && mypy src/pyautopsy/core</automated>
  </verify>
  <acceptance_criteria>
    - `src/pyautopsy/core/analyze.py` contains `def run_analyze(` and `class AnalyzeResult` and `class AnalyzeError`
    - `grep -n "run_ingest\|run_walk\|build_timeline\|assemble_report_body" src/pyautopsy/core/analyze.py` matches all four (composition, not duplication)
    - `python -m pytest tests/test_analyze.py::test_analyze_produces_reports tests/test_analyze.py::test_analyze_composes_pipeline -x -q` exits 0
    - re-running analyze into a dir that already has case.db raises AnalyzeError (fresh-case test passes)
    - `grep -n "import pytsk3\|import pyewf" src/pyautopsy/core/analyze.py` returns nothing (composes the seam via orchestrators, does not import it)
  </acceptance_criteria>
  <done>run_analyze composes ingest+walk+timeline+report in one process with fresh-case guard, analyze.start/end audit, segregated run metadata, and an analytical-only AnalyzeResult; test_analyze.py green.</done>
</task>

<task type="auto">
  <name>Task 2: analyze Typer command + CLI-02 byte-identical reproducibility test green</name>
  <files>src/pyautopsy/cli/main.py, tests/test_reproducibility.py</files>
  <read_first>
    - src/pyautopsy/cli/main.py (ingest command main.py:65-134 — image/--case/--examiner/--evidence-id/--acquisition-hash; walk command main.py:137-205 — --timezone/--max-hash-size, timezone validation main.py:180-185, forensic-error→typer.Exit mapping main.py:187-196, _INTEGRITY_EXIT_CODE main.py:40, summary echo main.py:198-205; orchestrator imports main.py:25-28)
    - src/pyautopsy/core/analyze.py (run_analyze/AnalyzeError/AnalyzeResult, from Task 1)
    - tests/test_reproducibility.py (_RUN_METADATA_COLUMNS:27, _ingest helper:30-45, two-runs structure:75-93, test_run_metadata_is_segregated:96-120)
    - tests/conftest.py (tiny_ext4_image fixture:53)
    - .planning/phases/03-timeline-mvp-report/03-RESEARCH.md (CLI-02 byte-identical test design lines 513-519; Open Question 2 HTML comparison lines 461-463)
    - .planning/phases/03-timeline-mvp-report/03-PATTERNS.md (cli/main.py assignment lines 175-186; test_reproducibility EXTEND lines 210-224)
  </read_first>
  <behavior>
    - `pyautopsy analyze <image> --case ...` exits 0 on a good fixture and writes reports/report.html + reports/report.json; forensic failures map to the integrity exit code AFTER the orchestrator FAIL audit.
    - The command surface mirrors ingest (image, --case, --examiner, --evidence-id, --acquisition-hash) plus walk knobs (--timezone, --max-hash-size); --timezone is validated up front.
    - `ingest` and `walk` still work standalone (no regression).
    - The CLI-02 test from 03-00 now PASSES: two analyze runs on tiny_ext4_image into separate dirs → byte-identical report.json (and report.html body), non-empty body sanity, and segregated run-metadata generation timestamp that IS present and DOES differ.
  </behavior>
  <action>
In src/pyautopsy/cli/main.py add `from pyautopsy.core.analyze import AnalyzeError, run_analyze` alongside existing orchestrator imports (main.py:25-28). Add an `@app.command()` `analyze` function copying the `walk` command shell (main.py:137-205): `Annotated[...]` Typer args/options mirroring ingest's image/--case/--examiner/--evidence-id/--acquisition-hash (main.py:66-97) PLUS walk's --timezone/--max-hash-size (main.py:155-168) — exactly the D-21 surface. Up-front timezone validation block (`ZoneInfo(timezone)` in try/except → usage error, main.py:180-185). Wrap `run_analyze(...)` in try/except catching `(AnalyzeError, IntegrityError, MountedSourceError, ImageOpenError, ...)` and `raise typer.Exit(code=_INTEGRITY_EXIT_CODE)` AFTER the orchestrator's FAIL audit (main.py:187-196). Echo a concise multi-line summary including the report output paths (report.html + report.json) (main.py:198-205). Keep ingest/walk commands unchanged.

Bring the CLI-02 test green: ensure the `_analyze(image, case)` CliRunner helper and `test_two_analyze_runs_byte_identical_report` added in 03-00 now pass — `(case_a/"reports"/"report.json").read_bytes() == (case_b/"reports"/"report.json").read_bytes()` on tiny_ext4_image (strict byte equality), with the non-empty body sanity assert (mirror test_reproducibility.py:90-93) and the negative segregation assert (run-metadata generation timestamp present and differs between runs). For HTML: per Open Question 2 option (b), assert whole-file byte-equality on report.html IF run metadata is a separate file; otherwise strip the labeled run-metadata footer block from both before comparing bodies — implement whichever matches the 03-02 reporter's actual run-metadata placement. If a determinism leak is found (a wall-clock value or unstable ordering in the body), fix it in the reporter/builder code path it originates from — not by loosening the test.
  </action>
  <verify>
    <automated>python -m pytest tests/test_analyze.py tests/test_reproducibility.py tests/test_cli_smoke.py -x -q && python -m pytest -q && ruff check . && mypy src</automated>
  </verify>
  <acceptance_criteria>
    - `src/pyautopsy/cli/main.py` contains `def analyze` registered via `@app.command()` and imports `run_analyze`
    - `python -m pytest tests/test_reproducibility.py::test_two_analyze_runs_byte_identical_report -x -q` exits 0 (the gating CLI-02 check)
    - `python -m pytest tests/test_cli_smoke.py tests/test_ingest.py tests/test_walk.py -q` exits 0 (ingest/walk no regression)
    - full suite + lint + types clean: `python -m pytest -q && ruff check . && mypy src` exits 0
    - `pyautopsy analyze --help` lists --case, --examiner, --evidence-id, --acquisition-hash, --timezone, --max-hash-size (D-21 surface)
  </acceptance_criteria>
  <done>analyze command exposed with the D-21 surface; ingest/walk unaffected; two analyze runs produce byte-identical report.json (and html body) with segregated run metadata; full suite + ruff + mypy green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args (--case, --timezone, image path) → orchestrator | User-supplied; validated up front; forensic failures must exit non-zero, not silently. |
| composed pipeline → case.db | analyze must not clobber/append to an existing case (reproducibility integrity). |
| two runs → report bytes | Any wall-clock/ordering leak into the body breaks the defensibility guarantee. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Repudiation | non-reproducible report | mitigate | CLI-02 byte-identical test on report.json (gating); fix any leak at source (no test loosening); run metadata segregated (D-25); D-26 total order. |
| T-03-12 | Tampering | analyze on existing case | mitigate | Fail loudly (AnalyzeError) if case.db exists — fresh-case semantics (A2); prevents append-and-confuse. |
| T-03-13 | Repudiation | missing self-audit | mitigate | analyze.start/analyze.end (+ FAIL/crashed) audit events like ingest/walk (REPORT-02); confined to case dir. |
| T-03-14 | Information Disclosure | forensic failure silently passing | mitigate | Forensic exceptions mapped to _INTEGRITY_EXIT_CODE after FAIL audit; the Phase 1 end-of-run re-verify still runs inside run_ingest (D-21). |
| T-03-03 | Tampering | D-14 native seam | accept | analyze composes orchestrators; imports no pytsk3/pyewf (grep gate). |
</threat_model>

<verification>
- `python -m pytest -q && ruff check . && mypy src` all green (the phase gate).
- CLI-02 byte-identical test passes on tiny_ext4_image; run metadata segregated and differs.
- ingest/walk standalone commands unaffected.
- analyze exposes the full D-21 option surface and writes both reports under case_dir/reports/.
</verification>

<success_criteria>
- One command produces timeline + HTML + JSON report from an image (CLI-01).
- Two runs → byte-identical report bodies with segregated run metadata (CLI-02).
- Phase 1 re-verify runs inside the composed pipeline; failures exit non-zero.
- No native imports in analyze; ingest/walk intact.
</success_criteria>

<output>
Create `.planning/phases/03-timeline-mvp-report/03-03-SUMMARY.md` when done.
</output>
