---
phase: 03-timeline-mvp-report
plan: 01
type: execute
wave: 1
depends_on: ["03-00"]
files_modified:
  - src/pyautopsy/timeline/__init__.py
  - src/pyautopsy/timeline/builder.py
autonomous: true
requirements: [TIME-01]
must_haves:
  truths:
    - "build_timeline reads files rows and writes one timeline_events row per populated MACB timestamp (D-24)"
    - "A None/zero MACB value produces NO event"
    - "ts_utc is copied verbatim from the walk's *_utc columns (never re-derived)"
    - "build_timeline writes through CaseStore inside a transaction and imports no native bindings"
  artifacts:
    - path: "src/pyautopsy/timeline/builder.py"
      provides: "build_timeline producer (FileRow → TimelineEvent explosion)"
      exports: ["build_timeline"]
      min_lines: 40
    - path: "src/pyautopsy/timeline/__init__.py"
      provides: "package export of build_timeline"
      contains: "build_timeline"
  key_links:
    - from: "src/pyautopsy/timeline/builder.py"
      to: "CaseStore.get_files / insert_timeline_events"
      via: "read files, explode MACB, bulk insert in transaction"
      pattern: "insert_timeline_events"
    - from: "build_timeline"
      to: "FileRow.*_utc columns"
      via: "verbatim copy of mtime_utc/atime_utc/ctime_utc/crtime_utc"
      pattern: "mtime_utc"
---

<objective>
Build the first producer into the shared forensic-event table (TIME-01 / D-23 / D-24): `timeline/builder.py` reads Phase 2 `files` rows and explodes each populated MACB timestamp into a normalized `timeline_events` row. This is the thinnest end-to-end-meaningful slice after the schema — it turns inventory into a queryable timeline that the reporter (Wave 2) consumes.

Purpose: The MACB-explosion producer is the architectural keystone (ARCHITECTURE Pattern 1) Phase 5 reuses additively; getting its shape right keeps the super-timeline schema-churn-free.
Output: `build_timeline(store, evidence_source_id) -> int` and a `timeline` package.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-timeline-mvp-report/03-RESEARCH.md
@.planning/phases/03-timeline-mvp-report/03-PATTERNS.md
@.planning/phases/03-timeline-mvp-report/03-00-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build_timeline producer — MACB explosion into timeline_events</name>
  <files>src/pyautopsy/timeline/__init__.py, src/pyautopsy/timeline/builder.py</files>
  <read_first>
    - src/pyautopsy/core/walk.py (_build_file_row mapper walk.py:371-421 incl. uid/gid reads; run_walk orchestrator shell walk.py:498-610 — transaction + audit + analytical-only result; "does not import pytsk3" note walk.py:36-37; WalkResult walk.py:237-256)
    - src/pyautopsy/case/store.py (get_files store.py:408-421; insert_timeline_events / get_timeline_events added in 03-00; transaction() store.py:155-182)
    - src/pyautopsy/case/models.py (FileRow fields models.py:124-148; TimelineEvent added in 03-00)
    - src/pyautopsy/core/__init__.py and timeline package expectations
    - .planning/phases/03-timeline-mvp-report/03-RESEARCH.md (MACB explosion code example lines 377-400; Pattern 2 lines 237-250; "Don't Hand-Roll" — read *_utc verbatim lines 270-271)
    - .planning/phases/03-timeline-mvp-report/03-PATTERNS.md (builder assignment lines 89-97)
  </read_first>
  <behavior>
    - For a FileRow with all four `*_utc` populated, explode() yields 4 TimelineEvents with event_type modified/accessed/changed/born.
    - A None `*_utc` column yields no event for that column.
    - source is `filesystem:<fs_type>` when fs_type is set, else `filesystem`; actor encodes uid/gid as `uid=<n>,gid=<n>` when either is known, else None (A4).
    - event carries file_id, meta_addr, volume_id, volume_offset, path copied from the FileRow; ts_utc is the *_utc string verbatim.
    - build_timeline reads all files for the evidence source, explodes them, inserts inside one `with store.transaction():`, returns the event count.
  </behavior>
  <action>
Create src/pyautopsy/timeline/__init__.py exporting `build_timeline` (mirror src/pyautopsy/core/__init__.py). Create src/pyautopsy/timeline/builder.py with `from __future__ import annotations` and a module docstring stating it reads store rows only and imports no native bindings (cf. walk.py:36-37).

Define a module-level MACB map `_MACB = (("mtime_utc","modified"),("atime_utc","accessed"),("ctime_utc","changed"),("crtime_utc","born"))` and a pure `_explode(file_row: FileRow) -> list[TimelineEvent]` per 03-RESEARCH.md:377-400: derive `actor` from uid/gid (None when both unknown), then for each `(col, etype)` read `getattr(file_row, col)`, skip when `None` (D-24: zero/None ⇒ no event — the walk already mapped 0 epoch → None, so no extra logic), else append a `TimelineEvent(evidence_source_id=file_row.evidence_source_id, file_id=file_row.id, ts_utc=<the *_utc string verbatim>, source=("filesystem:"+file_row.fs_type) if file_row.fs_type else "filesystem", event_type=etype, volume_id=file_row.volume_id, volume_offset=file_row.volume_offset, path=file_row.path, meta_addr=file_row.meta_addr, actor=actor)`. Do NOT reformat or re-derive timestamps from epochs.

Define `build_timeline(store: CaseStore, evidence_source_id: int) -> int`: read `store.get_files(evidence_source_id)`, build the event list via `_explode`, then `with store.transaction(): store.insert_timeline_events(events)`; return `len(events)`. The producer never imports pytsk3/pyewf and contains no raw SQL (writes through the store). Keep it analytical-only (no wall-clock).
  </action>
  <verify>
    <automated>python -m pytest tests/test_timeline.py -x -q && ruff check src/pyautopsy/timeline && mypy src/pyautopsy/timeline</automated>
  </verify>
  <acceptance_criteria>
    - `src/pyautopsy/timeline/builder.py` contains `def build_timeline(` and `def _explode(`
    - `python -m pytest tests/test_timeline.py::test_macb_explosion tests/test_timeline.py::test_total_order tests/test_timeline.py::test_ext4_timeline -x -q` exits 0
    - `grep -n "import pytsk3\|import pyewf\|datetime.now\|from_epoch_utc\|iso_utc" src/pyautopsy/timeline/builder.py` returns nothing (no native seam, no timestamp re-derivation)
    - `grep -v '^#' src/pyautopsy/timeline/builder.py | grep -c "execute(\|executemany(" ` is 0 (no raw SQL — writes via store)
    - `mypy src/pyautopsy/timeline` exits 0
  </acceptance_criteria>
  <done>build_timeline explodes MACB columns into D-24-shaped timeline_events through the store transaction; test_timeline.py green; no native imports, no timestamp re-derivation, no raw SQL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| files rows → timeline_events rows | Path/fs_type strings (attacker-controlled) are copied into event rows; they are not interpreted here, only persisted via parameterized insert. |
| walk output (*_utc) → timeline | Timezone correctness was established + tested in Phase 2; the builder must not re-derive it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | timestamp provenance | mitigate | Copy `*_utc` strings verbatim; never re-derive from epochs — preserves Phase 2's tested FAT/zero-epoch handling (grep gate forbids from_epoch_utc/iso_utc in builder). |
| T-03-05 | Information Disclosure (misleading) | dropped events | mitigate | D-24 explosion is total over populated columns; exact-count Nyquist assertions in test_timeline guard against silently dropping events. |
| T-03-03 | Tampering | D-14 native seam | accept | Pure-Python producer; verified by grep (no pytsk3/pyewf import). |
</threat_model>

<verification>
- `python -m pytest tests/test_timeline.py -q` green (explosion, total order via store read, ext4 integration).
- `ruff check src/pyautopsy/timeline && mypy src/pyautopsy/timeline` clean.
- builder imports no native bindings and re-derives no timestamps.
</verification>

<success_criteria>
- build_timeline produces one event per populated MACB time, none for None.
- ts_utc copied verbatim; actor/source encoded per D-23.
- Events persisted through the store transaction; readable in D-26 order.
</success_criteria>

<output>
Create `.planning/phases/03-timeline-mvp-report/03-01-SUMMARY.md` when done.
</output>
