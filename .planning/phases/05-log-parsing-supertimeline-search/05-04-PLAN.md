---
phase: 05-log-parsing-supertimeline-search
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - src/pyautopsy/core/analyze.py
  - src/pyautopsy/cli/main.py
  - src/pyautopsy/report/assemble.py
  - tests/test_reproducibility.py
autonomous: true
requirements: [LOG-01, LOG-02, LOG-03, LOG-04, TIME-02, SEARCH-01, SEARCH-02]

must_haves:
  truths:
    - "An examiner runs `pyautopsy analyze <image> --case <dir> --logs --search <term>` and the single report contains the merged super-timeline (fs + log events), a log-findings section, and a search-results section"
    - "Default `analyze` (no --logs/--search) produces a report body byte-identical to the Phase-4 baseline (CLI-02, D-48)"
    - "Two analyze runs with --logs on the same fixture produce byte-identical report bodies, including tied log events (same ts, NULL meta) in a stable order (CR-01)"
    - "The super-timeline section is the existing get_timeline_events read with no new ORDER BY (TIME-02)"
  artifacts:
    - path: "src/pyautopsy/core/analyze.py"
      provides: "opt-in --logs/--search arms wired into run_analyze (mirror recover/filter opt-in)"
      contains: "run_logs"
    - path: "src/pyautopsy/report/assemble.py"
      provides: "log-findings + search-results sections; body byte-stable"
      contains: "get_search_hits"
    - path: "tests/test_reproducibility.py"
      provides: "tied-log-event stability + default-analyze-unchanged regression tests"
      contains: "test_tied_log_events_stable"
  key_links:
    - from: "src/pyautopsy/core/analyze.py"
      to: "src/pyautopsy/core/logs.py::run_logs"
      via: "opt-in arm run only when --logs supplied"
      pattern: "if logs|run_logs"
    - from: "src/pyautopsy/report/assemble.py"
      to: "src/pyautopsy/case/store.py::get_timeline_events"
      via: "super-timeline section reads the store-owned total order (TIME-02)"
      pattern: "get_timeline_events"
---

<objective>
Wave 3 integration slice: wire the Wave-1/2 log and search capabilities into the single
`analyze` command and the report, then lock determinism. After this plan an examiner runs
`pyautopsy analyze <image> --case <dir> --logs --search <term>` and gets ONE report containing
the merged super-timeline (filesystem + log events in one UTC order), a log-findings section,
and a search-results section — while default `analyze` stays byte-identical to the Phase-4
baseline (CLI-02, D-48).

This closes TIME-02 (the super-timeline is the existing get_timeline_events read, now carrying
log events) and proves CLI-02 reproducibility for the new sections, including the CR-01 tied-log-
event trap (same ts, NULL fs meta). All four log requirements + both search requirements are
listed here because this plan is where they become observable in the end-to-end report — but
their implementation lives in 05-01/02/03; this plan verifies the closure and determinism.

Purpose: deliver the full "image + logs → defensible report" pipeline as one command with
byte-stable output.
Output: analyze opt-in wiring, the `analyze --logs/--search` flags, the report sections, and the
reproducibility/super-timeline regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-log-parsing-supertimeline-search/05-RESEARCH.md
@.planning/phases/05-log-parsing-supertimeline-search/05-PATTERNS.md
@src/pyautopsy/core/analyze.py
@src/pyautopsy/report/assemble.py
</context>

## Phase Goal

**As an** examiner, **I want to** see filesystem and log evidence merged into one UTC-ordered
super-timeline and search across allocated, unallocated, and file content, **so that** I get a
defensible "image + logs → report" pipeline.

## Artifacts this phase produces (Wave 3 portion)

- `src/pyautopsy/core/analyze.py` (MOD): `logs: bool` + `search: str | None` opt-in arms in `run_analyze`; new flags recorded in the `analyze.start` audit; counts in `AnalyzeResult` (no wall-clock)
- `src/pyautopsy/cli/main.py` (MOD): `analyze --logs` / `--search` flags
- `src/pyautopsy/report/assemble.py` (MOD): log-findings + search-results sections; super-timeline section via get_timeline_events; body byte-stable
- `tests/test_reproducibility.py` (MOD): `test_tied_log_events_stable`, `test_default_analyze_unchanged` (extended)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: wire --logs/--search opt-in into analyze + report sections</name>
  <files>src/pyautopsy/core/analyze.py, src/pyautopsy/cli/main.py, src/pyautopsy/report/assemble.py</files>
  <read_first>
    - src/pyautopsy/core/analyze.py (lines ~174, ~253-299: `run_analyze`'s `recover: bool` + `filter_requested` opt-in gates and the `if recover:` / `if filter_requested:` arms to mirror; the analyze.start audit at ~254-264; the run_metadata.json sidecar at ~185)
    - src/pyautopsy/cli/main.py (the `analyze` command + its existing opt-in flags `--recover`/`--nsrl`/`--hash-set-*` to mirror for `--logs`/`--search`)
    - src/pyautopsy/report/assemble.py (the existing timeline + known-filter sections + the body-byte-determinism docstring — the analog for the new sections; confirm it already reads via store-owned total orders)
    - src/pyautopsy/core/logs.py (run_logs signature from 05-01) and src/pyautopsy/core/search.py (run_search signature from 05-03) — the orchestrators to call
    - .planning/phases/05-log-parsing-supertimeline-search/05-RESEARCH.md (§Pitfall 6 report-body byte-determinism; §Pattern 5 TIME-02 is the existing read)
    - .planning/phases/05-log-parsing-supertimeline-search/05-PATTERNS.md (Pattern Assignments: core/analyze.py + report/assemble.py)
  </read_first>
  <behavior>
    - run_analyze gains `logs: bool=False` and `search: str | None=None`; `run_logs` runs ONLY when `logs` is set, `run_search` ONLY when `search` is set, each forwarding `evidence_source_id=source_id`; default analyze (neither set) takes the identical path as Phase-4
    - the new flags are recorded in the analyze.start audit; all new counts live in AnalyzeResult with NO wall-clock
    - assemble.py gains a super-timeline section (the existing get_timeline_events read = TIME-02, now including log events), a log-findings section (tamperability + completeness findings), and a search-results section (via get_search_hits) — all read through store-owned total orders; NO utc_now() in the body; volatile run metadata stays in run_metadata.json
    - CLI exposes `analyze --logs` / `--search <term>` mirroring the recover/nsrl opt-in flags
  </behavior>
  <action>
    In core/analyze.py add `logs: bool=False` and `search: str | None=None` parameters to
    run_analyze and add the opt-in arms next to the existing recover/filter arms (analyze.py
    ~283-299), calling `run_logs(...)`/`run_search(...)` with `evidence_source_id=source_id` ONLY
    when the respective flag is set — keeping the default path byte-identical to Phase-4 (D-48).
    Record the new flags in the analyze.start audit (~254-264); keep all counts in AnalyzeResult
    (no wall-clock). In report/assemble.py add a super-timeline section reading
    `store.get_timeline_events(source_id)` (= TIME-02; add NO new ORDER BY), a log-findings section
    (tamperability + log-completeness findings surfaced honestly), and a search-results section
    reading the new `store.get_search_hits(source_id)`; derive every section from sorted store data
    only, with NO `utc_now()` in the body (Pitfall 6) — volatile run metadata stays in the existing
    run_metadata.json sidecar. Add the `--logs`/`--search` flags to the `analyze` Typer command in
    cli/main.py mirroring the recover/nsrl opt-in pattern.
  </action>
  <verify>
    <automated>python -m pytest tests/test_analyze.py tests/test_report.py tests/test_cli_smoke.py -q && ruff check src/pyautopsy/core/analyze.py src/pyautopsy/report/assemble.py && mypy src/pyautopsy/core/analyze.py src/pyautopsy/report/assemble.py</automated>
  </verify>
  <acceptance_criteria>
    - `pyautopsy analyze --logs --search <term>` produces one report with super-timeline + log-findings + search-results sections
    - run_logs/run_search run ONLY when their flag is set; default analyze path is unchanged
    - the super-timeline section reads get_timeline_events with no new ORDER BY (TIME-02)
    - assemble.py body has no utc_now(); new sections derive from store-owned total orders only
    - tests/test_analyze.py, tests/test_report.py, tests/test_cli_smoke.py pass; ruff + mypy clean
  </acceptance_criteria>
  <done>The single analyze command surfaces logs + search + the merged super-timeline opt-in, with the report body still sourced entirely from store-owned ordered reads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reproducibility + super-timeline determinism regression tests</name>
  <files>tests/test_reproducibility.py</files>
  <read_first>
    - tests/test_reproducibility.py (the existing two-run byte-identical body assertion + the default-analyze-unchanged baseline test to extend)
    - src/pyautopsy/case/store.py (get_timeline_events D-26 total order + the `…source, actor, id` tiebreak that handles NULL meta — the CR-01 mechanism under test)
    - .planning/phases/05-log-parsing-supertimeline-search/05-RESEARCH.md (§Pitfall 3 CR-01 NULL-key tie; §Validation Architecture CR-01 + default-analyze-unchanged rows)
    - .planning/phases/05-log-parsing-supertimeline-search/05-VALIDATION.md (Wave 0 — the tied-log-event fixture this test consumes)
  </read_first>
  <behavior>
    - test_tied_log_events_stable: runs `analyze --logs` (or run_logs) twice on the tied-event fixture (two log events at the identical second, NULL fs meta) and asserts the report body / get_timeline_events order is byte-identical across runs — proving the source/actor/id tiebreak (CR-01) holds for log events
    - test_default_analyze_unchanged (extended): asserts default analyze (no --logs/--search) report body is byte-identical to the committed Phase-4 baseline (D-48/CLI-02)
    - both rely on deterministic insert order: rotated files oldest→newest, registry-declared parser order, single insert transaction
  </behavior>
  <action>
    Extend tests/test_reproducibility.py with `test_tied_log_events_stable`: on the Wave-0
    tied-event fixture, run the log pipeline twice and assert the report body (and/or
    get_timeline_events output) is byte-identical across both runs, exercising the CR-01
    NULL-meta tiebreak for log events. Extend/confirm `test_default_analyze_unchanged`: assert the
    default `analyze` report body (no --logs/--search) is byte-identical to the committed Phase-4
    baseline body (D-48 — the opt-in flags must not perturb the default path). Use the existing
    two-run comparison helper in this file; do not introduce wall-clock into assertions.
  </action>
  <verify>
    <automated>python -m pytest tests/test_reproducibility.py -q && python -m pytest -q && ruff check tests/test_reproducibility.py</automated>
  </verify>
  <acceptance_criteria>
    - test_tied_log_events_stable passes: two runs on tied log events (same ts, NULL meta) yield byte-identical ordered output (CR-01)
    - test_default_analyze_unchanged passes: default analyze body is byte-identical to the Phase-4 baseline (D-48/CLI-02)
    - the FULL suite (`python -m pytest`) is GREEN — every Phase-5 requirement test now passes
    - ruff clean on the test file
  </acceptance_criteria>
  <done>Determinism is locked: log events and the merged super-timeline are byte-stable across runs, and default analyze output is unchanged — the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| log/search results → report body | Untrusted evidence strings rendered into HTML; report body must stay byte-deterministic and injection-safe. |
| examiner CLI flags → run_analyze | Opt-in flags must not alter the default deterministic path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | wall-clock / unstable order leaking into report body | mitigate | New sections read store-owned total orders only; no utc_now() in body; run metadata in the sidecar (Pitfall 6); regression tests assert byte-identity. |
| T-05-04-02 | Tampering | evidence strings injected into HTML report | mitigate | jinja2 autoescape (already on) neutralizes log/search strings; snippets are bounded data. |
| T-05-04-03 | Integrity | inferred time/order presented as fact | mitigate | D-46 flags carried into the log-findings section; tamperability + completeness surfaced as findings, never intent. |
| T-05-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; tests/test_no_new_deps.py blocks any new runtime dep (D-43). Package Legitimacy Audit: not applicable (zero external packages). |
</threat_model>

<verification>
- `python -m pytest` FULL suite GREEN (all LOG-01/02/03/04, TIME-02, SEARCH-01/02 tests pass)
- `python -m pytest tests/test_reproducibility.py -q` GREEN (tied-log-event + default-analyze-unchanged)
- `ruff check src/pyautopsy && mypy src/pyautopsy` clean
- `pyautopsy analyze --logs --search <term>` emits one report with super-timeline + log-findings + search-results sections; default analyze body byte-identical to Phase-4
</verification>

<success_criteria>
- The single analyze command delivers the full "image + logs → report" pipeline (logs + search opt-in)
- TIME-02: merged super-timeline via the existing get_timeline_events read (no new ORDER BY)
- CLI-02/D-48: default analyze byte-stable; new sections byte-deterministic; tied log events stable (CR-01)
- All 7 phase requirements observable end-to-end; full suite green
</success_criteria>

<output>
Create `.planning/phases/05-log-parsing-supertimeline-search/05-04-SUMMARY.md` when done
</output>
