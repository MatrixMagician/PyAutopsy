---
phase: 05-log-parsing-supertimeline-search
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/pyautopsy/log/syslog.py
  - src/pyautopsy/log/shell_history.py
autonomous: true
requirements: [LOG-02, LOG-03]

must_haves:
  truths:
    - "An examiner running `pyautopsy logs` sees syslog/messages service, kernel, cron, and error events normalized into the timeline alongside auth events"
    - "Per-user .bash_history and .zsh_history commands are parsed; zsh extended and bash #<epoch> timestamps are used when present, otherwise the event is flagged 'no per-line timestamp' and a tamperability finding is surfaced"
    - "Both parsers register into the existing LogParser registry and flow through the unchanged core/logs.py orchestrator (EXT-01 — no orchestrator edit)"
  artifacts:
    - path: "src/pyautopsy/log/syslog.py"
      provides: "LOG-02 syslog/messages parser (RFC3164 grammar reuse + program/PID + level/facility heuristics)"
      min_lines: 40
    - path: "src/pyautopsy/log/shell_history.py"
      provides: "LOG-03 bash/zsh history parser + no-timestamp fallback + tamperability finding"
      min_lines: 40
  key_links:
    - from: "src/pyautopsy/log/syslog.py"
      to: "src/pyautopsy/log/registry.py"
      via: "registers as a LogParser in declared order"
      pattern: "register"
    - from: "src/pyautopsy/log/shell_history.py"
      to: "src/pyautopsy/log/timeresolve.py"
      via: "file-mtime fallback for the no-per-line-timestamp case"
      pattern: "timeresolve|mtime"
---

<objective>
Wave 2 vertical slice (parser breadth): extend the Wave-1 log pipeline to the remaining two
in-scope log types. After this plan `pyautopsy logs` additionally surfaces syslog/messages
events (service, kernel, cron, error) and per-user shell history (bash/zsh), all flowing through
the SAME registry → discover → timeresolve → normalize → insert pipeline built in Wave 1 —
demonstrating the EXT-01 plugin design (new parsers add without touching the orchestrator).

Shell history honesty is a first-class requirement (D-44, Success Criterion 2): history has no
reliable per-line timestamps and is trivially editable, so the parser emits an explicit
no-timestamp flag + a tamperability finding rather than asserting chronological truth or intent.

This plan touches ONLY two new files under log/ (disjoint from the Wave-2 search slice 05-03),
so the two run in parallel.

Purpose: complete LOG-02 + LOG-03 by reusing the proven Wave-1 pipeline.
Output: log/syslog.py and log/shell_history.py, both registered LogParsers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-log-parsing-supertimeline-search/05-RESEARCH.md
@.planning/phases/05-log-parsing-supertimeline-search/05-PATTERNS.md
@src/pyautopsy/log/auth.py
@src/pyautopsy/log/registry.py
</context>

## Phase Goal

**As an** examiner, **I want to** see filesystem and log evidence merged into one UTC-ordered
super-timeline and search across allocated, unallocated, and file content, **so that** I get a
defensible "image + logs → report" pipeline.

## Artifacts this phase produces (Wave 2 / log portion)

- `src/pyautopsy/log/syslog.py` (NEW): `SyslogParser` implementing `LogParser` (LOG-02)
- `src/pyautopsy/log/shell_history.py` (NEW): `ShellHistoryParser` implementing `LogParser` (LOG-03) + tamperability finding

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: syslog/messages parser (LOG-02)</name>
  <files>src/pyautopsy/log/syslog.py</files>
  <read_first>
    - src/pyautopsy/log/auth.py (the Wave-1 sibling parser — copy its RFC3164/RFC5424 grammar usage, registry registration, ParsedRecord emission, and unmatched-line-still-emitted discipline)
    - src/pyautopsy/log/registry.py (the `LogParser` Protocol + `ParsedRecord` shape + ordered registration to mirror)
    - .planning/phases/05-log-parsing-supertimeline-search/05-RESEARCH.md (§Pattern 2 RFC3164/RFC5424 grammar; LOG-02 row in §Phase Requirements — program/PID + level/facility heuristics)
    - .planning/phases/05-log-parsing-supertimeline-search/05-PATTERNS.md (Pattern Assignments: log/syslog.py — "same as log/auth.py")
  </read_first>
  <behavior>
    - parses /var/log/syslog and /var/log/messages RFC3164 lines (reusing the Wave-1 grammar), extracting program, optional PID, and the message
    - classifies service / kernel (program `kernel`) / cron (program `CRON`/`cron`) / error events via small honest heuristics, mapped to descriptive action/outcome (observed-line-only, never inferred intent)
    - a line matching no known shape still becomes an event (action=None, raw msg in attributes), never dropped
    - registers `SyslogParser` into the registry in declared order (after auth) so core/logs.py picks it up with NO orchestrator change
    - RFC5424 lines parsed directly; RFC3164 lines routed through timeresolve (D-46)
  </behavior>
  <action>
    Create log/syslog.py implementing the `LogParser` protocol exactly like log/auth.py. Reuse the
    shared RFC3164/RFC5424 compiled grammar (import the patterns from log/auth.py or a shared module
    if Wave 1 factored them out — do not redefine divergent regexes). Add a small program-based
    classification (kernel/cron/service) and an error-level heuristic, emitting `ParsedRecord`s with
    `source="syslog"` and honest descriptive action/outcome. Skip blank lines tolerantly
    (filter/hashsets parse_hash_set style); a non-matching line still yields an event with the raw
    msg in attributes. Register `SyslogParser` in the registry in declared order. Decode evidence
    bytes utf-8/errors="replace". No native imports, no raw SQL.
  </action>
  <verify>
    <automated>python -m pytest tests/test_logs.py::test_syslog_events -x -q && python -m pytest tests/test_seam_allowlist.py -q && ruff check src/pyautopsy/log/syslog.py && mypy src/pyautopsy/log/syslog.py</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_logs.py::test_syslog_events passes: service, kernel, cron, and error events parsed from the fixture syslog with honest action/outcome
    - SyslogParser is registered in the registry and consumed by the UNCHANGED core/logs.py (no orchestrator edit in this plan)
    - Non-matching lines still produce events (raw msg in attributes); blank lines skipped
    - tests/test_seam_allowlist.py passes; ruff + mypy clean on the file
  </acceptance_criteria>
  <done>syslog/messages events flow through the existing log pipeline and merge into the timeline, proving the EXT-01 add-a-parser-without-touching-the-orchestrator design.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: shell history parser (LOG-03) with tamperability finding</name>
  <files>src/pyautopsy/log/shell_history.py</files>
  <read_first>
    - src/pyautopsy/log/auth.py (sibling parser registration + ParsedRecord emission to mirror)
    - src/pyautopsy/log/timeresolve.py (the file-mtime fallback + flagging shape for the no-per-line-timestamp case)
    - src/pyautopsy/log/registry.py (the `LogParser` Protocol + ordered registration)
    - .planning/phases/05-log-parsing-supertimeline-search/05-RESEARCH.md (§Pitfall 2 shell-history has no reliable per-line timestamps; LOG-03 row in §Phase Requirements)
    - .planning/phases/05-log-parsing-supertimeline-search/05-PATTERNS.md (Pattern Assignments: log/shell_history.py — format detection + tamperability finding)
  </read_first>
  <behavior>
    - detects zsh extended history (`: <start>:<elapsed>;<command>`) → uses the embedded epoch; bash `#<epoch>` lines preceding a command → uses that epoch
    - when neither timestamp form is present, emits the event with `ts_utc` = the history file's mtime and flags `attributes["ts_basis"] = "file-mtime-fallback; no per-line timestamp"` (never silent)
    - surfaces a tamperability finding (D-44 / Success Criterion 2): history is editable and order is not chronological truth — recorded as a finding, never as asserted intent
    - per-user: the parsed user (derived from the /home/<user> path) becomes `actor = f"user={name}"` when known, else None
    - registers `ShellHistoryParser` in the registry in declared order
  </behavior>
  <action>
    Create log/shell_history.py implementing the `LogParser` protocol. Detect format per Pitfall 2:
    zsh extended (`: \d+:\d+;`) → embedded epoch via timeresolve.from-epoch path; bash `#<epoch>`
    pairs → that epoch; otherwise NO per-line timestamp → emit with `ts_utc` = the history file's
    mtime and flag `ts_basis="file-mtime-fallback; no per-line timestamp"`. Derive the owning user
    from the /home/<user> path for `actor`. Emit a tamperability finding object (history is editable;
    order is not chronological — D-44 honesty, observed-fact-only, never intent). Register
    `ShellHistoryParser` in the registry in declared order. Decode evidence bytes
    utf-8/errors="replace". No native imports, no raw SQL.
  </action>
  <verify>
    <automated>python -m pytest tests/test_logs.py::test_shell_history_tamperability -x -q && python -m pytest tests/test_seam_allowlist.py -q && ruff check src/pyautopsy/log/shell_history.py && mypy src/pyautopsy/log/shell_history.py</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_logs.py::test_shell_history_tamperability passes: zsh extended + bash #<epoch> timestamps used when present; otherwise event flagged "no per-line timestamp" with file-mtime fallback
    - A tamperability finding is surfaced (D-44) — observed-fact-only, no asserted intent
    - actor reflects the owning /home/<user>; None when unknown (never "user=None")
    - ShellHistoryParser is registered and consumed by the unchanged core/logs.py
    - tests/test_seam_allowlist.py passes; ruff + mypy clean
  </acceptance_criteria>
  <done>Per-user shell history is parsed with honest timestamp handling and a tamperability finding, completing LOG-03 through the shared pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| on-disk log/history text → parser | Untrusted evidence bytes decoded as DATA only; no write path. |
| history file order/timestamps → finding | History is examiner-editable evidence; never treated as authoritative chronology or intent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | malicious syslog/history content | mitigate | Decode utf-8/errors="replace" as data; report HTML autoescape (already on) neutralizes evidence strings. |
| T-05-02-02 | Repudiation/Integrity | shell-history presented as chronological truth | mitigate | D-44 — emit tamperability + no-per-line-timestamp findings; mtime fallback flagged; never assert order or intent. |
| T-05-02-03 | Information disclosure | inferred tz/year on syslog presented as fact | mitigate | Reuse Wave-1 timeresolve D-46 flagging (timestamp_source + assumed zone/year). |
| T-05-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; tests/test_no_new_deps.py blocks any new runtime dep (D-43). Package Legitimacy Audit: not applicable (zero external packages). |
</threat_model>

<verification>
- `python -m pytest tests/test_logs.py -q` GREEN (syslog + shell-history tests now pass alongside Wave-1)
- `python -m pytest tests/test_seam_allowlist.py tests/test_no_new_deps.py -q` GREEN
- `ruff check src/pyautopsy/log && mypy src/pyautopsy/log` clean
- core/logs.py is UNCHANGED by this plan (EXT-01 verified — parsers add via registry only)
</verification>

<success_criteria>
- LOG-02: syslog/messages service, kernel, cron, error events parsed and normalized
- LOG-03: bash/zsh history parsed with honest timestamp handling + tamperability finding
- Both parsers register into the existing pipeline with no orchestrator edit (EXT-01)
- Seam-allowlist and no-new-dep invariants hold
</success_criteria>

<output>
Create `.planning/phases/05-log-parsing-supertimeline-search/05-02-SUMMARY.md` when done
</output>
