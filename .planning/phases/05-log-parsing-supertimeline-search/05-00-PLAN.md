---
phase: 05-log-parsing-supertimeline-search
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/fixtures/make_fixtures.py
  - tests/conftest.py
  - tests/test_logs.py
  - tests/test_search.py
  - tests/test_supertimeline.py
  - tests/test_no_new_deps.py
autonomous: true
requirements: [LOG-01, LOG-02, LOG-03, LOG-04, TIME-02, SEARCH-01, SEARCH-02]

must_haves:
  truths:
    - "A committed, deterministic ext4 fixture image carries a rotated/gz auth.log set, syslog, per-user bash/zsh history, an /etc/localtime symlink + /etc/timezone, and planted strings in allocated + unallocated space"
    - "Failing (RED) test stubs exist for every Phase-5 requirement so each later slice has an automated gate to turn green"
    - "A regression test fails the build if Phase 5 adds any new runtime dependency to pyproject.toml [project.dependencies] (D-43)"
  artifacts:
    - path: "tests/fixtures/make_fixtures.py"
      provides: "Fixture builder extended to plant log + search content (host-only mkfs/debugfs; committed image)"
      contains: "auth.log"
    - path: "tests/test_logs.py"
      provides: "RED stubs for LOG-01/02/03/04, D-45/D-46, TIME-02"
      min_lines: 40
    - path: "tests/test_search.py"
      provides: "RED stubs for SEARCH-01/02 incl. boundary-spanning + unallocated"
      min_lines: 30
    - path: "tests/test_no_new_deps.py"
      provides: "D-43 no-new-runtime-dependency guard"
      contains: "dependencies"
  key_links:
    - from: "tests/conftest.py"
      to: "tests/fixtures/*.img"
      via: "pytest fixture exposing the log-bearing image path + ground-truth constants"
      pattern: "fixture"
---

<objective>
Wave 0 of Phase 5. Build the committed, deterministic test fixtures and the failing (RED)
test stubs that every later vertical slice turns green. This is the test-first foundation:
no production code ships in this plan, but the entire requirement surface (LOG-01/02/03/04,
TIME-02, SEARCH-01/02, D-43/D-45/D-46) gets an executable gate.

Per the MVP vertical-slice rule, the first deliverable of the phase is the end-to-end RED
test surface plus the evidence fixtures those tests assert against. Fixtures are built once
on a host with mkfs/debugfs and committed (CI never runs mkfs — mirrors the Phase-2/4 fixture
approach already in tests/fixtures/).

Purpose: give Waves 1-3 a continuous automated feedback signal and lock the no-new-dependency
invariant (D-43) before any log/search code exists.
Output: extended make_fixtures.py + committed fixture image(s), conftest fixtures, and RED
stub test files for logs, search, super-timeline, and the deps guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-log-parsing-supertimeline-search/05-RESEARCH.md
@.planning/phases/05-log-parsing-supertimeline-search/05-VALIDATION.md
@.planning/phases/05-log-parsing-supertimeline-search/05-PATTERNS.md
</context>

## Phase Goal

**As an** examiner, **I want to** see filesystem and log evidence merged into one UTC-ordered
super-timeline and search across allocated, unallocated, and file content, **so that** I get a
defensible "image + logs → report" pipeline.

## Artifacts this phase produces (Wave 0 portion)

- `tests/fixtures/make_fixtures.py` (MOD): plants the log + search corpus into a committed ext4 image
- New committed fixture image(s) under `tests/fixtures/` carrying the log/search corpus
- `tests/conftest.py` (MOD): pytest fixtures exposing the log-bearing image + ground-truth constants
- `tests/test_logs.py` (NEW): RED stubs — `test_auth_taxonomy`, `test_rfc3164_grammar`, `test_syslog_events`, `test_shell_history_tamperability`, `test_normalize_to_timeline_event`, `test_timeresolve_inferred_and_flagged`, `test_rotation_reassembly_order`, `test_super_timeline_merge`
- `tests/test_search.py` (NEW): RED stubs — `test_streaming_search_all_regions`, `test_boundary_spanning_match`, `test_ioc_and_hash_hits`
- `tests/test_supertimeline.py` (NEW): RED stub — `test_super_timeline_merge` (or extends test_timeline.py)
- `tests/test_no_new_deps.py` (NEW): D-43 guard

<tasks>

<task type="auto">
  <name>Task 1: Extend fixture builder + conftest to plant the log and search corpus</name>
  <files>tests/fixtures/make_fixtures.py, tests/conftest.py</files>
  <read_first>
    - tests/fixtures/make_fixtures.py (the existing builder — copy its mkfs/debugfs + committed-image idiom verbatim; this is the analog)
    - tests/conftest.py (existing pytest fixtures exposing fixture image paths + ground-truth constants)
    - .planning/phases/05-log-parsing-supertimeline-search/05-RESEARCH.md (§Validation Architecture > Wave 0 Gaps; §Common Pitfalls 1-5; Assumptions A2/A4/A5 — what each fixture must exercise)
    - .planning/phases/05-log-parsing-supertimeline-search/05-VALIDATION.md (Wave 0 Requirements list)
  </read_first>
  <action>
    Extend tests/fixtures/make_fixtures.py to build and COMMIT a deterministic ext4 image
    (reuse the existing mkfs/debugfs host-only build idiom already in this file — do NOT build
    at test time) carrying: (1) a rotated auth log set /var/log/auth.log, /var/log/auth.log.1,
    /var/log/auth.log.2.gz with RFC3164 lines covering ssh Accepted/Failed-password, sudo
    granted + authentication failure, and PAM session opened/closed — spanning a Dec→Jan year
    boundary across the rotated set (exercises D-46 year inference + Pitfall 4 ordering);
    (2) /var/log/syslog with service/kernel/cron/error lines (LOG-02); (3) per-user
    /home/<user>/.bash_history with bare lines AND a `#<epoch>` HISTTIMEFORMAT pair, and
    /home/<user>/.zsh_history with extended `: <ts>:<dur>;cmd` lines (LOG-03 / Pitfall 2);
    (4) an /etc/localtime SYMLINK to /usr/share/zoneinfo/<Zone> plus an /etc/timezone text file
    (D-46 / Assumption A5); (5) a known literal string planted in ALLOCATED file content, the
    SAME or a distinct known string planted in UNALLOCATED space (write a file, delete it, leave
    its blocks — mirror the ext4_orphan.img technique already in this builder), a known IOC term,
    and a file whose MD5/SHA-1/SHA-256 are a known-bad-hash target (SEARCH-01/02 / Assumption A4);
    (6) two log events at the IDENTICAL second with NULL fs meta (CR-01 tied-order regression,
    Pitfall 3). Record every ground-truth constant (planted offsets, the inferred zone, the
    expected year, the hash hex, the IOC term, the tied-event count) as module-level constants
    or a committed JSON sidecar. In tests/conftest.py add pytest fixtures returning the
    log-bearing image path and exposing those ground-truth constants (mirror the existing
    tiny_ext4/ext4_orphan fixture functions). Reads in tests stay read-only; the builder never
    mutates a committed image at test time.
  </action>
  <verify>
    <automated>python tests/fixtures/make_fixtures.py && python -c "import pathlib,glob; assert glob.glob('tests/fixtures/*log*.img') or glob.glob('tests/fixtures/*.img')" && python -m pytest tests/conftest.py -q --collect-only 2>/dev/null; echo done</automated>
  </verify>
  <acceptance_criteria>
    - make_fixtures.py builds (host-side) a committed ext4 image containing all six corpora above; the image file exists under tests/fixtures/ after running the builder
    - The builder reuses the existing mkfs/debugfs idiom in the same file (no new build dependency, no test-time mutation)
    - Ground-truth constants (inferred zone, expected year, planted allocated + unallocated offsets, known-bad hash hex, IOC term, tied-event count) are recorded as importable constants or a committed sidecar
    - conftest.py exposes a fixture yielding the log-bearing image path + the ground-truth constants, callable from tests/test_logs.py and tests/test_search.py
  </acceptance_criteria>
  <done>The committed log/search fixture image and its ground-truth constants exist and are importable by the stub tests; building it is byte-deterministic and read-only at test time.</done>
</task>

<task type="auto">
  <name>Task 2: Write RED test stubs for logs, super-timeline, search, and the no-new-deps guard</name>
  <files>tests/test_logs.py, tests/test_search.py, tests/test_supertimeline.py, tests/test_no_new_deps.py</files>
  <read_first>
    - tests/test_timeline.py (analog for super-timeline / get_timeline_events assertions and the FileRow→TimelineEvent expectations)
    - tests/test_knownfiles.py (analog for hash-match assertions reused by SEARCH-02)
    - tests/test_reproducibility.py (analog for the CR-01 tied-order + byte-stable two-run assertion shape)
    - .planning/phases/05-log-parsing-supertimeline-search/05-RESEARCH.md (§Validation Architecture > Phase Requirements → Test Map — the exact test names + behaviors to stub)
    - .planning/phases/05-log-parsing-supertimeline-search/05-PATTERNS.md (Key existing signatures index — the symbols the stubs reference)
  </read_first>
  <action>
    Create the four test files with the exact test names from RESEARCH §Validation Architecture
    Test Map, importing the Wave-0 fixtures from Task 1. In tests/test_logs.py add:
    test_rfc3164_grammar, test_auth_taxonomy (LOG-01), test_syslog_events (LOG-02),
    test_shell_history_tamperability (LOG-03 — asserts the no-per-line-timestamp fallback flag +
    a tamperability finding), test_normalize_to_timeline_event (LOG-04 — asserts the produced
    TimelineEvent carries source/action/outcome/actor/file_id), test_timeresolve_inferred_and_flagged
    (D-46 — asserts tz inferred from /etc/localtime, year inferred, BOTH flagged in attributes,
    UTC fallback warned), test_rotation_reassembly_order (D-45/Pitfall 4 — oldest→newest + a
    completeness finding). In tests/test_supertimeline.py (or extend test_timeline.py) add
    test_super_timeline_merge (TIME-02 — fs + log events returned by get_timeline_events in one
    UTC total order). In tests/test_search.py add test_streaming_search_all_regions (SEARCH-01 —
    literal+regex over allocated + file content + UNALLOCATED with correct offsets),
    test_boundary_spanning_match (Pitfall 5 — a match straddling a chunk boundary found once),
    test_ioc_and_hash_hits (SEARCH-02 — IOC + known-bad-hash hits by file+offset). In
    tests/test_no_new_deps.py assert pyproject.toml [project.dependencies] is byte-identical to a
    committed Phase-4 baseline snapshot (read the current `dependencies = [...]` block and compare
    to the stored baseline string — D-43). All log/search/timeline tests reference symbols that do
    not yet exist (log.*, search.*, store.insert_search_hits, iter_unallocated_blocks) so they FAIL
    (RED) — that is the required state; mark none as xfail/skip. The no-new-deps test PASSES now and
    must keep passing (it is a guard, not a slice gate).
  </action>
  <verify>
    <automated>python -m pytest tests/test_no_new_deps.py -q && python -m pytest tests/test_logs.py tests/test_search.py tests/test_supertimeline.py -q 2>&1 | grep -Eq 'error|failed|no tests ran' && echo "RED-as-expected"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_no_new_deps.py PASSES (current deps match the committed baseline; adding any dep would fail it)
    - tests/test_logs.py, tests/test_search.py, tests/test_supertimeline.py exist with every named test from the RESEARCH Test Map and are RED (collection or assertion failure because log/search symbols are absent) — none xfail/skipped
    - Every stub references the real eventual symbol (e.g. `from pyautopsy.log...`, `store.insert_search_hits`, `iter_unallocated_blocks`) so turning it green requires the actual slice
    - `python -m pytest tests/test_seam_allowlist.py -q` still passes (no new pytsk3 importers introduced by tests)
  </acceptance_criteria>
  <done>All Phase-5 requirement behaviors have a named, RED automated test gating a later slice; the D-43 deps guard is green and will block any new runtime dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host build → committed fixture | Fixture image is built once on a trusted host (mkfs/debugfs) and committed; CI consumes it read-only. No untrusted input at this stage. |
| test process → committed image | Tests open the committed image read-only; no test-time mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-00-01 | Tampering | committed fixture image | accept | Built host-side from a deterministic script, reviewed in PR; CI never builds it. Low risk — synthetic test data only. |
| T-05-00-02 | Tampering | test-time source mutation | mitigate | Builder is host-only; tests open images read-only (no mount, no write path) — same discipline as existing fixture tests. |
| T-05-00-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; tests/test_no_new_deps.py asserts zero new runtime deps (D-43). RESEARCH Package Legitimacy Audit: not applicable (zero external packages this phase). |
</threat_model>

<verification>
- `python tests/fixtures/make_fixtures.py` rebuilds the committed corpus deterministically (host-side)
- `python -m pytest tests/test_no_new_deps.py -q` is GREEN
- `python -m pytest tests/test_logs.py tests/test_search.py tests/test_supertimeline.py -q` is RED (symbols absent — expected)
- `python -m pytest tests/test_seam_allowlist.py -q` still GREEN
</verification>

<success_criteria>
- Committed log/search fixture image exists with all six corpora and recorded ground-truth constants
- Every Phase-5 requirement (LOG-01/02/03/04, TIME-02, SEARCH-01/02) has a named RED test
- D-43 no-new-dependency guard is green
- No production code added; seam allowlist unaffected
</success_criteria>

<output>
Create `.planning/phases/05-log-parsing-supertimeline-search/05-00-SUMMARY.md` when done
</output>
